---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - src/auth.ts
  - src/middleware.ts
  - src/models/User.ts
  - src/lib/mongodb.ts
  - src/lib/validations.ts
  - src/actions/auth.register.ts
  - src/app/api/auth/[...nextauth]/route.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
must_haves:
  truths:
    - "NextAuth v5 con Credentials Provider está configurado y exporta handlers, signIn, signOut, auth"
    - "El middleware protege /tma/* — sin sesión redirige a /login"
    - "La raíz / redirige a /tma si hay sesión, a /login si no"
    - "El modelo User tiene email único + passwordHash (nunca almacena password en plaintext)"
    - "connectDB() usa global caching para evitar agotar conexiones Atlas M0"
    - "registerUser verifica allowlist ANTES de consultar la DB (T6 mitigado)"
    - "registerUser con email fuera de allowlist retorna { error: 'Este email no está autorizado' } y NO crea usuario"
    - "registerUser con email duplicado retorna { error: 'Este email ya tiene cuenta, iniciá sesión' }"
    - "Registro exitoso hace auto-login y redirige a /tma (D-04)"
    - "bcryptjs.compare siempre ejecuta aunque el usuario no exista (anti timing attack)"
  artifacts:
    - path: "src/auth.ts"
      provides: "Config NextAuth v5"
      exports: ["handlers", "signIn", "signOut", "auth"]
    - path: "src/middleware.ts"
      provides: "Protección de rutas + redirect de /"
      contains: "startsWith(\"/tma\")"
    - path: "src/models/User.ts"
      provides: "Mongoose schema usuarios"
      exports: ["default User model"]
    - path: "src/lib/mongodb.ts"
      provides: "Conexión con global caching"
      exports: ["connectDB"]
    - path: "src/lib/validations.ts"
      provides: "Zod schemas para login y registro"
      exports: ["loginSchema", "registerSchema"]
    - path: "src/actions/auth.register.ts"
      provides: "Server Action de registro con allowlist + auto-login"
      contains: "ALLOWLIST"
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "NextAuth API handler"
      contains: "handlers"
  key_links:
    - from: "src/middleware.ts"
      to: "src/auth.ts"
      via: "import { auth } from \"@/auth\""
      pattern: "from \"@/auth\""
    - from: "src/actions/auth.register.ts"
      to: "src/auth.ts"
      via: "import { signIn } from \"@/auth\""
      pattern: "signIn.*credentials"
    - from: "src/auth.ts"
      to: "src/lib/mongodb.ts"
      via: "connectDB() en authorize callback"
      pattern: "connectDB"
    - from: "src/auth.ts"
      to: "src/models/User.ts"
      via: "User.findOne() en authorize"
      pattern: "User.findOne"
---

<objective>
Implementar la capa de autenticación completa: configuración NextAuth v5, modelo User en Mongoose, conexión MongoDB con global caching, schemas Zod, Server Action de registro con validación de allowlist y auto-login, y middleware de protección de rutas.

Purpose: Es el núcleo de seguridad de la app — sin esto, cualquier persona puede acceder a /tma. Define los contratos de auth que todas las fases siguientes consumen.

Output: Sistema de auth funcional donde solo los 5 emails de NyC pueden registrarse, las rutas /tma/* están protegidas, y un registro exitoso crea sesión automáticamente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Allowlist exacta (D-09) — HARDCODED, nunca en env vars: -->
<!-- nsilva@nyc.com.ar -->
<!-- crivera@nyc.com.ar -->
<!-- tderosa@nyc.com.ar -->
<!-- vespinosa@nyc.com.ar -->
<!-- ekoch@nyc.com.ar -->

<!-- Mensajes de error exactos (D-02, D-10, UI-SPEC copywriting): -->
<!-- Fuera de allowlist: "Este email no está autorizado" -->
<!-- Email duplicado: "Este email ya tiene cuenta, iniciá sesión" -->
<!-- Credenciales inválidas: "Credenciales inválidas" (genérico — no revela si email existe) -->

<!-- Estructura de directorios (D-15, 01-RESEARCH.md): -->
<!-- src/auth.ts (NO src/app/auth.ts — debe ser importable como @/auth desde middleware Edge) -->
<!-- src/middleware.ts (NO src/app/middleware.ts) -->

<!-- CRÍTICO — Edge Runtime en middleware: -->
<!-- El middleware corre en Edge Runtime. Mongoose NO puede usarse en middleware. -->
<!-- Solo usar import { auth } de @/auth (JWT-based, Edge-compatible). -->
<!-- Nunca importar mongoose ni connectDB en middleware.ts. -->

<!-- CRÍTICO — signIn() en Server Action: -->
<!-- signIn("credentials", { redirectTo: "/tma" }) lanza NEXT_REDIRECT. -->
<!-- En el try/catch, re-lanzar todo excepto AuthError: -->
<!--   catch (error) { if (error instanceof AuthError) return { error }; throw error } -->
<!-- Sin esto, el redirect de registro exitoso se bloquea. -->

<!-- Patrón global caching MongoDB (01-RESEARCH.md Pattern 5): -->
<!-- declare global { var mongoose: { conn: ..., promise: ... } } -->
<!-- let cached = global.mongoose -->
<!-- if (!cached) cached = global.mongoose = { conn: null, promise: null } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Capa de datos y configuración NextAuth v5</name>
  <files>
    src/models/User.ts
    src/lib/mongodb.ts
    src/lib/validations.ts
    src/auth.ts
    src/app/api/auth/[...nextauth]/route.ts
  </files>

  <read_first>
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Pattern 1 (auth.ts completo), §Pattern 3 (API route), §Pattern 4 (registerUser — para entender qué llama authorize), §Pattern 5 (mongodb.ts), §Code Examples §Mongoose User Model, §Zod validation schemas
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Common Pitfalls Pitfall 1 (versión next-auth), Pitfall 2 (Edge Runtime), Pitfall 5 (signIn redirect)
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Security Domain §Known Threat Patterns
  </read_first>

  <action>
Crear los siguientes archivos en el directorio `tma/src/`:

---

**`tma/src/models/User.ts`**:
```typescript
import mongoose, { Schema, Document } from "mongoose"

export interface IUser extends Document {
  email: string
  passwordHash: string
  createdAt: Date
}

const UserSchema = new Schema<IUser>({
  email: {
    type: String,
    required: true,
    unique: true,
    lowercase: true,
    trim: true,
  },
  passwordHash: { type: String, required: true },
  createdAt: { type: Date, default: Date.now },
})

export default mongoose.models.User || mongoose.model<IUser>("User", UserSchema)
```

---

**`tma/src/lib/mongodb.ts`**:
```typescript
import mongoose from "mongoose"

const MONGODB_URI = process.env.MONGODB_URI!

if (!MONGODB_URI) {
  throw new Error("MONGODB_URI no está definido en las variables de entorno")
}

declare global {
  // eslint-disable-next-line no-var
  var mongoose: {
    conn: typeof import("mongoose") | null
    promise: Promise<typeof import("mongoose")> | null
  }
}

let cached = global.mongoose

if (!cached) {
  cached = global.mongoose = { conn: null, promise: null }
}

export async function connectDB() {
  if (cached.conn) return cached.conn

  if (!cached.promise) {
    cached.promise = mongoose.connect(MONGODB_URI, {
      bufferCommands: false,
      serverSelectionTimeoutMS: 5000,
    })
  }

  cached.conn = await cached.promise
  return cached.conn
}
```

---

**`tma/src/lib/validations.ts`**:
```typescript
import { z } from "zod"

export const loginSchema = z.object({
  email: z.string().email("Email inválido"),
  password: z.string().min(1, "La contraseña es requerida"),
})

export const registerSchema = z.object({
  email: z.string().email("Email inválido"),
  password: z
    .string()
    .min(8, "La contraseña debe tener al menos 8 caracteres"),
})

export type LoginSchema = z.infer<typeof loginSchema>
export type RegisterSchema = z.infer<typeof registerSchema>
```

---

**`tma/src/auth.ts`** (IMPORTANTE: en `src/auth.ts`, NO en `src/app/auth.ts`):
```typescript
import NextAuth from "next-auth"
import Credentials from "next-auth/providers/credentials"
import { connectDB } from "@/lib/mongodb"
import User from "@/models/User"
import bcryptjs from "bcryptjs"
import { loginSchema } from "@/lib/validations"
import { AuthError } from "next-auth"

export const { handlers, signIn, signOut, auth } = NextAuth({
  providers: [
    Credentials({
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" },
      },
      authorize: async (credentials) => {
        const parsed = await loginSchema.safeParseAsync(credentials)
        if (!parsed.success) throw new Error("Credenciales inválidas")
        const { email, password } = parsed.data

        await connectDB()

        // SEGURIDAD T1: Siempre ejecutar bcryptjs.compare aunque el usuario
        // no exista, para evitar timing attack de enumeración.
        const user = await User.findOne({ email }).lean()
        const dummyHash =
          "$2a$12$dummyhashtopreventtimingattackonusernotfound"
        const passwordHash = user?.passwordHash ?? dummyHash

        const valid = await bcryptjs.compare(password, passwordHash)

        if (!user || !valid) {
          throw new Error("Credenciales inválidas")
        }

        return { id: user._id.toString(), email: user.email }
      },
    }),
  ],
  callbacks: {
    jwt({ token, user }) {
      if (user) token.id = user.id
      return token
    },
    session({ session, token }) {
      if (token.id) session.user.id = token.id as string
      return session
    },
  },
  pages: {
    signIn: "/login",
  },
})
```

---

**`tma/src/app/api/auth/[...nextauth]/route.ts`** (crear el directorio anidado si no existe):
```typescript
import { handlers } from "@/auth"
export const { GET, POST } = handlers
```

---

Verificar que el directorio `tma/src/app/api/auth/[...nextauth]/` exista antes de crear el archivo. Usar el Write tool directamente con la ruta completa — Next.js crea los directorios intermedios en tiempo de build, pero el archivo debe existir físicamente.
  </action>

  <verify>
    <automated>cd tma && npm run build 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `tma/src/auth.ts` contiene `export const { handlers, signIn, signOut, auth } = NextAuth(`
    - `tma/src/auth.ts` contiene `bcryptjs.compare(password, passwordHash)` (timing-safe)
    - `tma/src/auth.ts` contiene `const dummyHash` (anti timing attack T1)
    - `tma/src/auth.ts` contiene `pages: { signIn: "/login" }`
    - `tma/src/auth.ts` import es `from "@/lib/mongodb"` y `from "@/models/User"` (no imports directos de mongoose)
    - `tma/src/lib/mongodb.ts` contiene `global.mongoose = { conn: null, promise: null }` (global caching)
    - `tma/src/lib/mongodb.ts` contiene `serverSelectionTimeoutMS: 5000`
    - `tma/src/models/User.ts` contiene `unique: true` en el campo email
    - `tma/src/models/User.ts` contiene `lowercase: true` en el campo email
    - `tma/src/models/User.ts` NO contiene campo `password` — solo `passwordHash` (AUTH-04 — nunca plaintext)
    - `tma/src/lib/validations.ts` contiene `min(8` en registerSchema (D-03)
    - `tma/src/app/api/auth/[...nextauth]/route.ts` contiene `export const { GET, POST } = handlers`
    - `npm run build` sale con código 0
  </acceptance_criteria>

  <done>NextAuth v5 configurado con Credentials Provider y timing-safe authorize. User model sin campo password plaintext. connectDB() con global caching. Zod schemas con validación de contraseña mínimo 8 chars. API route handler one-liner.</done>
</task>

<task type="auto">
  <name>Tarea 2: Server Action de registro + middleware de protección</name>
  <files>
    src/actions/auth.register.ts
    src/middleware.ts
  </files>

  <read_first>
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Pattern 2 (middleware.ts completo), Pattern 4 (registerUser completo), §Common Pitfalls Pitfall 2 (Edge Runtime), Pitfall 5 (signIn re-lanza NEXT_REDIRECT)
    - `.planning/phases/01-foundation/01-CONTEXT.md` §Decisions D-04 (registro → auto-login → /tma), D-05 (/ → redirect), D-09 (allowlist exacta), D-10 (mensaje error allowlist), D-11 (rutas /tma/*)
    - `tma/src/auth.ts` — verificar los exports disponibles (signIn, auth)
    - `tma/src/models/User.ts` — verificar la interfaz IUser
  </read_first>

  <action>
Crear los siguientes archivos:

---

**`tma/src/actions/auth.register.ts`**:
```typescript
"use server"

import { signIn } from "@/auth"
import { connectDB } from "@/lib/mongodb"
import User from "@/models/User"
import bcryptjs from "bcryptjs"
import { registerSchema } from "@/lib/validations"
import { AuthError } from "next-auth"

// Allowlist fija en código (D-09) — exactamente estos 5 emails
const ALLOWLIST = [
  "nsilva@nyc.com.ar",
  "crivera@nyc.com.ar",
  "tderosa@nyc.com.ar",
  "vespinosa@nyc.com.ar",
  "ekoch@nyc.com.ar",
]

export async function registerUser(
  _: unknown,
  formData: FormData
): Promise<{ error?: string } | void> {
  // 1. Validar formato con Zod
  const parsed = registerSchema.safeParse({
    email: formData.get("email"),
    password: formData.get("password"),
  })

  if (!parsed.success) {
    const first = parsed.error.errors[0]
    return { error: first?.message ?? "Datos inválidos" }
  }

  const { email, password } = parsed.data

  // 2. Verificar allowlist ANTES de consultar DB (T6 — AUTH-02, D-10)
  if (!ALLOWLIST.includes(email.toLowerCase())) {
    return { error: "Este email no está autorizado" }
  }

  // 3. Verificar email único (D-02)
  await connectDB()
  const existing = await User.findOne({ email: email.toLowerCase() }).lean()
  if (existing) {
    return { error: "Este email ya tiene cuenta, iniciá sesión" }
  }

  // 4. Hash de contraseña (AUTH-04) — NUNCA loggear password
  const passwordHash = await bcryptjs.hash(password, 12)
  await User.create({
    email: email.toLowerCase(),
    passwordHash,
    createdAt: new Date(),
  })

  // 5. Auto-login tras registro exitoso (D-04)
  // CRÍTICO: signIn lanza NEXT_REDIRECT — re-lanzar todo excepto AuthError
  try {
    await signIn("credentials", { email, password, redirectTo: "/tma" })
  } catch (error) {
    if (error instanceof AuthError) {
      return { error: "Error al iniciar sesión automáticamente. Intentá loguearte manualmente." }
    }
    throw error // Re-lanzar NEXT_REDIRECT para que Next.js procese el redirect
  }
}
```

---

**`tma/src/middleware.ts`** (IMPORTANTE: en `src/middleware.ts`, NO en `src/app/middleware.ts`):
```typescript
import { auth } from "@/auth"
import { NextResponse } from "next/server"

export default auth((req) => {
  const isLoggedIn = !!req.auth
  const { pathname } = req.nextUrl

  // D-05: / redirige según estado de sesión
  if (pathname === "/") {
    return NextResponse.redirect(
      new URL(isLoggedIn ? "/tma" : "/login", req.url)
    )
  }

  // D-11 / AUTH-06: Rutas bajo /tma/* requieren sesión activa
  if (pathname.startsWith("/tma") && !isLoggedIn) {
    return NextResponse.redirect(new URL("/login", req.url))
  }

  return NextResponse.next()
})

export const config = {
  // Aplicar middleware a todas las rutas excepto assets estáticos y api/auth
  matcher: ["/((?!api/auth|_next/static|_next/image|favicon.ico).*)"],
}
```

NOTA sobre Edge Runtime: `auth` de `@/auth` es Edge-compatible porque usa JWT cookies. NO importar `mongoose`, `connectDB`, ni ningún módulo Node.js en este archivo — causa crash en Edge Runtime.
  </action>

  <verify>
    <automated>cd tma && npm run build 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `tma/src/actions/auth.register.ts` primera línea es `"use server"`
    - `tma/src/actions/auth.register.ts` contiene el array `ALLOWLIST` con exactamente 5 emails (incluyendo `vespinosa@nyc.com.ar`)
    - `tma/src/actions/auth.register.ts` contiene `if (!ALLOWLIST.includes(email.toLowerCase()))` ANTES de `await connectDB()` (verificación allowlist antes de DB — T6)
    - `tma/src/actions/auth.register.ts` contiene `"Este email no está autorizado"` (D-10, AUTH-02)
    - `tma/src/actions/auth.register.ts` contiene `"Este email ya tiene cuenta, iniciá sesión"` (D-02)
    - `tma/src/actions/auth.register.ts` contiene `bcryptjs.hash(password, 12)` (AUTH-04)
    - `tma/src/actions/auth.register.ts` NO contiene `console.log(password)` ni logging de la contraseña en plaintext (T5)
    - `tma/src/actions/auth.register.ts` contiene `if (error instanceof AuthError)` seguido de `throw error` (Pitfall 5)
    - `tma/src/middleware.ts` contiene `import { auth } from "@/auth"` (NO de `next-auth/middleware`)
    - `tma/src/middleware.ts` contiene `pathname.startsWith("/tma")` (D-11, AUTH-06)
    - `tma/src/middleware.ts` contiene redirect a `/login` cuando `!isLoggedIn` en ruta /tma
    - `tma/src/middleware.ts` contiene redirect a `/tma` cuando `isLoggedIn` en ruta `/`
    - `tma/src/middleware.ts` NO contiene imports de `mongoose` ni `connectDB` (evita crash Edge Runtime)
    - `npm run build` sale con código 0
  </acceptance_criteria>

  <done>Server Action de registro con allowlist verificada antes de DB, hash bcrypt, auto-login con manejo correcto de NEXT_REDIRECT. Middleware Edge-compatible que protege /tma/* y redirige / según sesión. Todos los mensajes de error en español según specs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | formData con email y password cruza a registerUser() — entrada no confiable |
| Server Action → MongoDB | Datos validados + hasheados antes de inserción |
| Edge Middleware → NextAuth JWT | Middleware lee cookie JWT sin acceso a DB |
| authorize callback → MongoDB | Credenciales de login verificadas contra hash en DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | authorize callback (auth.ts) | mitigate | bcryptjs.compare siempre ejecuta con dummyHash cuando el usuario no existe — no hay short-circuit que revele si el email está registrado (anti-timing attack T1) |
| T-02-02 | Information Disclosure | AUTH_SECRET | mitigate | AUTH_SECRET referenciado solo como `process.env.AUTH_SECRET` — nunca hardcodeado. NextAuth lanza MissingSecretError si no está definido. Agregar a Vercel Dashboard antes de deploy. |
| T-02-03 | Elevation of Privilege | registerUser allowlist check | mitigate | Verificación de allowlist ocurre como PRIMER paso, antes de cualquier consulta a DB — un email no autorizado nunca llega a tocar MongoDB (T6 mitigado) |
| T-02-04 | Tampering | CSRF en Server Actions | accept | Next.js 15 implementa protección CSRF automática en Server Actions via Same-Origin Policy + token interno — no requiere implementación manual (T3) |
| T-02-05 | Elevation of Privilege | Session fixation | accept | NextAuth v5 rota el session token en cada sign-in por diseño — no requiere acción adicional (T4) |
| T-02-06 | Information Disclosure | Password en logs | mitigate | registerUser hashea password antes de cualquier operación y nunca lo pasa a console.log. Solo `passwordHash` se persiste en DB (T5) |
  </threat_model>

<verification>
```bash
# Desde el directorio tma/
cd tma

# 1. Build limpio
npm run build

# 2. Verificar auth.ts exports
grep -c "handlers, signIn, signOut, auth" src/auth.ts

# 3. Verificar timing-safe en authorize
grep -c "dummyHash" src/auth.ts

# 4. Verificar allowlist completa (deben ser 5 emails)
grep -c "nyc.com.ar" src/actions/auth.register.ts

# 5. Verificar que allowlist check va ANTES de connectDB
# (allowlist antes de la línea con connectDB en el archivo)
grep -n "ALLOWLIST.includes\|connectDB" src/actions/auth.register.ts

# 6. Verificar middleware importa de @/auth (no next-auth/middleware)
grep -c "from \"@/auth\"" src/middleware.ts

# 7. Verificar que middleware NO importa mongoose
grep -c "mongoose\|connectDB" src/middleware.ts
# Debe retornar 0

# 8. Verificar re-throw de NEXT_REDIRECT
grep -c "throw error" src/actions/auth.register.ts
```
</verification>

<success_criteria>
- `npm run build` sale con código 0
- `src/auth.ts` exporta `{ handlers, signIn, signOut, auth }` y tiene timing-safe compare con dummyHash
- `src/middleware.ts` protege `/tma/*` y redirige `/` según sesión, importando de `@/auth` (Edge-compatible)
- `src/actions/auth.register.ts` verifica allowlist ANTES de conectar a DB, hashea con bcrypt cost 12, re-lanza NEXT_REDIRECT
- `src/models/User.ts` tiene campo `passwordHash` (no `password`) con unique email lowercase
- Los 5 mensajes de error en español están en el código exactamente como se especificaron en D-02, D-10
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/01-foundation/01-02-SUMMARY.md` siguiendo el template en `@$HOME/.claude/get-shit-done/templates/summary.md`.

Incluir en el summary:
- Confirmación de que el timing-safe pattern (dummyHash) fue implementado
- Cualquier ajuste necesario al pattern de signIn/AuthError
- Estado del build tras implementar auth completa
</output>
