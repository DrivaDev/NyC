---
phase: 04-casos-crud-dashboard
plan: "02"
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - tma/src/models/Caso.ts
  - tma/src/lib/validations.ts
  - tma/src/app/api/casos/route.ts
autonomous: true
requirements:
  - CASOS-01
  - CASOS-02
  - CASOS-03
  - CASOS-07

must_haves:
  truths:
    - "El modelo Caso.ts existe con los 4 campos obligatorios (nombre, fechaIngreso, fechaVencimiento, responsable)"
    - "casoSchema está exportado desde validations.ts y valida los 4 campos con mensajes en español"
    - "GET /api/casos retorna 401 sin sesión y 200 con array de casos con sesión"
    - "POST /api/casos retorna 401 sin sesión, 400 con campos vacíos, 201 con datos válidos"
    - "DELETE /api/casos retorna 401 sin sesión, 400 con id inválido, 200 al eliminar correctamente"
  artifacts:
    - path: "tma/src/models/Caso.ts"
      provides: "Mongoose model para asuntos jurídicos"
      exports: ["ICaso", "default (Caso model)"]
      contains: "mongoose.models.Caso || mongoose.model"
    - path: "tma/src/lib/validations.ts"
      provides: "Schema Zod para validación de asuntos"
      exports: ["casoSchema", "CasoSchema"]
      contains: "casoSchema"
    - path: "tma/src/app/api/casos/route.ts"
      provides: "Route Handler CRUD de asuntos"
      exports: ["GET", "POST", "DELETE"]
      contains: "force-dynamic"
  key_links:
    - from: "tma/src/app/api/casos/route.ts"
      to: "tma/src/models/Caso.ts"
      via: "import Caso from @/models/Caso"
      pattern: "import Caso from"
    - from: "tma/src/app/api/casos/route.ts"
      to: "tma/src/lib/validations.ts"
      via: "casoSchema.safeParse(body)"
      pattern: "casoSchema.safeParse"
    - from: "tma/src/app/api/casos/route.ts"
      to: "tma/src/lib/mongodb.ts"
      via: "await connectDB()"
      pattern: "connectDB"
---

<objective>
Implementar la capa de backend completa para el módulo Casos: modelo Mongoose, schema de validación Zod, y Route Handler con los tres métodos HTTP. Todos los handlers verifican sesión, la DB solo se toca si hay sesión activa.

Purpose: Proveer la API que consume el dashboard de casos del frontend. Una vez completado este plan, los tests de casosRoute.test.ts pasarán de RED a GREEN.
Output: Caso.ts, validations.ts extendido, route.ts con GET/POST/DELETE.
</objective>

<execution_context>
@C:\Users\Equipo\Documents\NyC\.planning\phases\04-casos-crud-dashboard\04-PATTERNS.md
</execution_context>

<context>
@C:\Users\Equipo\Documents\NyC\.planning\ROADMAP.md
@C:\Users\Equipo\Documents\NyC\.planning\phases\04-casos-crud-dashboard\04-CONTEXT.md
@C:\Users\Equipo\Documents\NyC\.planning\phases\04-casos-crud-dashboard\04-RESEARCH.md

<interfaces>
<!-- Analog exacto — tma/src/models/User.ts (patrón a replicar) -->
import mongoose, { Schema, Document } from "mongoose"

export interface IUser extends Document {
  email: string
  passwordHash: string
  createdAt: Date
}

const UserSchema = new Schema<IUser>({
  email: { type: String, required: true, unique: true, lowercase: true, trim: true },
  passwordHash: { type: String, required: true },
  createdAt: { type: Date, default: Date.now },
})

export default mongoose.models.User || mongoose.model<IUser>("User", UserSchema)
// GUARD OBLIGATORIO: mongoose.models.X || mongoose.model(...) — evita "Cannot overwrite model" en hot-reload

<!-- Validaciones existentes a preservar — tma/src/lib/validations.ts (COMPLETO) -->
import { z } from "zod"

export const loginSchema = z.object({
  email: z.string().email("Email inválido").transform(v => v.toLowerCase()),
  password: z.string().min(1, "La contraseña es requerida"),
})

export const registerSchema = z.object({
  email: z.string().email("Email inválido").transform(v => v.toLowerCase()),
  password: z.string().min(8, "La contraseña debe tener al menos 8 caracteres"),
})

export type LoginSchema = z.infer<typeof loginSchema>
export type RegisterSchema = z.infer<typeof registerSchema>
// AGREGAR casoSchema y CasoSchema AL FINAL — no modificar lo anterior

<!-- Patrón auth en Route Handler — analog: tma/src/app/api/contracts/analyze/route.ts -->
import { auth } from "@/auth"
const session = await auth()
if (!session) return NextResponse.json({ error: "No autorizado" }, { status: 401 })

// Directiva obligatoria anti-cache:
export const dynamic = "force-dynamic"
</interfaces>

<!-- Patrón de connectDB — tma/src/lib/mongodb.ts -->
<!-- import { connectDB } from "@/lib/mongodb" — usar este import exacto -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modelo Caso.ts + extensión de validations.ts</name>
  <read_first>
    tma/src/models/User.ts
    tma/src/lib/validations.ts
    tma/src/lib/mongodb.ts
    tma/src/__tests__/casos/casoSchema.test.ts
  </read_first>
  <files>
    tma/src/models/Caso.ts
    tma/src/lib/validations.ts
  </files>
  <behavior>
    - casoSchema.safeParse({ nombre: "Test", fechaIngreso: "2026-06-15", fechaVencimiento: "2026-12-31", responsable: "Rivera" }) → success: true
    - casoSchema.safeParse({ nombre: "", fechaIngreso: "2026-06-15", fechaVencimiento: "2026-12-31", responsable: "Rivera" }) → success: false, issues[0].message === "El nombre del asunto es obligatorio."
    - casoSchema.safeParse({ nombre: "Test", fechaIngreso: "", fechaVencimiento: "2026-12-31", responsable: "Rivera" }) → success: false
    - casoSchema.safeParse({ nombre: "Test", fechaIngreso: "2026-06-15", fechaVencimiento: "", responsable: "Rivera" }) → success: false
    - casoSchema.safeParse({ nombre: "Test", fechaIngreso: "2026-06-15", fechaVencimiento: "2026-12-31", responsable: "" }) → success: false, issues[0].message === "El responsable es obligatorio."
  </behavior>
  <action>
Crear `tma/src/models/Caso.ts`. Replicar exactamente el patrón de `User.ts` (guard `mongoose.models.Caso ||` obligatorio):

```typescript
import mongoose, { Schema, Document } from "mongoose"

export interface ICaso extends Document {
  nombre: string
  fechaIngreso: Date
  fechaVencimiento: Date
  responsable: string
  createdAt: Date
}

const CasoSchema = new Schema<ICaso>({
  nombre: { type: String, required: true, trim: true },
  fechaIngreso: { type: Date, required: true },
  fechaVencimiento: { type: Date, required: true },
  responsable: { type: String, required: true, trim: true },
  createdAt: { type: Date, default: Date.now },
})

export default mongoose.models.Caso || mongoose.model<ICaso>("Caso", CasoSchema)
```

Extender `tma/src/lib/validations.ts` agregando al final del archivo (DESPUÉS de las líneas existentes — no modificar loginSchema ni registerSchema):

```typescript
export const casoSchema = z.object({
  nombre: z.string().min(1, "El nombre del asunto es obligatorio."),
  fechaIngreso: z.string().min(1, "La fecha de ingreso es obligatoria."),
  fechaVencimiento: z.string().min(1, "La fecha de vencimiento es obligatoria."),
  responsable: z.string().min(1, "El responsable es obligatorio."),
})

export type CasoSchema = z.infer<typeof casoSchema>
```

Nota Zod v4: los mensajes de error se pasan como string directo en `.min(1, "mensaje")` — no como objeto `{ message: "..." }` como en Zod v3. El proyecto usa Zod v4.4.3.
  </action>
  <verify>
    <automated>cd tma && npm run test:ci -- --reporter=verbose src/__tests__/casos/casoSchema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tma/src/models/Caso.ts existe
    - Caso.ts contiene "export interface ICaso extends Document"
    - Caso.ts contiene "mongoose.models.Caso || mongoose.model"
    - Caso.ts contiene "fechaIngreso: { type: Date, required: true }"
    - Caso.ts contiene "fechaVencimiento: { type: Date, required: true }"
    - validations.ts contiene "export const casoSchema"
    - validations.ts contiene "El nombre del asunto es obligatorio."
    - validations.ts contiene "El responsable es obligatorio."
    - validations.ts aún contiene "loginSchema" y "registerSchema" (no se tocaron)
    - npm run test:ci -- src/__tests__/casos/casoSchema.test.ts sale con código 0 (todos los tests GREEN)
  </acceptance_criteria>
  <done>Caso.ts con 4 campos requeridos creado. casoSchema exportado desde validations.ts con los 4 mensajes de error en español. Tests casoSchema.test.ts pasando GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route Handler /api/casos con GET + POST + DELETE</name>
  <read_first>
    tma/src/app/api/contracts/analyze/route.ts
    tma/src/models/Caso.ts
    tma/src/lib/validations.ts
    tma/src/__tests__/casos/casosRoute.test.ts
  </read_first>
  <files>
    tma/src/app/api/casos/route.ts
  </files>
  <behavior>
    - GET sin sesión → 401 { error: "No autorizado" }
    - GET con sesión → 200 con array de casos ordenados por fechaVencimiento asc
    - POST sin sesión → 401
    - POST con campos vacíos → 400 { error: "Datos inválidos", issues: [...] }
    - POST con datos válidos → 201 con el caso creado (fechas convertidas a Date con T12:00:00)
    - DELETE sin sesión → 401
    - DELETE con id no-ObjectId → 400 { error: "ID inválido" }
    - DELETE con id válido y caso existente → 200 { ok: true }
    - DELETE con id válido pero caso no encontrado → 404 { error: "No encontrado" }
  </behavior>
  <action>
Crear el directorio `tma/src/app/api/casos/` y el archivo `route.ts`. El directorio puede no existir aún — crearlo al escribir el archivo.

Contenido completo de `tma/src/app/api/casos/route.ts`:

```typescript
import { NextRequest, NextResponse } from "next/server"
import { auth } from "@/auth"
import { connectDB } from "@/lib/mongodb"
import Caso from "@/models/Caso"
import { casoSchema } from "@/lib/validations"
import mongoose from "mongoose"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await auth()
  if (!session) {
    return NextResponse.json({ error: "No autorizado" }, { status: 401 })
  }

  await connectDB()
  const casos = await Caso.find().sort({ fechaVencimiento: 1 }).lean()
  return NextResponse.json(casos)
}

export async function POST(request: NextRequest) {
  const session = await auth()
  if (!session) {
    return NextResponse.json({ error: "No autorizado" }, { status: 401 })
  }

  const body = await request.json()
  const result = casoSchema.safeParse(body)
  if (!result.success) {
    return NextResponse.json(
      { error: "Datos inválidos", issues: result.error.issues },
      { status: 400 }
    )
  }

  await connectDB()
  const caso = await Caso.create({
    nombre: result.data.nombre,
    responsable: result.data.responsable,
    fechaIngreso: new Date(result.data.fechaIngreso + "T12:00:00"),
    fechaVencimiento: new Date(result.data.fechaVencimiento + "T12:00:00"),
  })
  return NextResponse.json(caso, { status: 201 })
}

export async function DELETE(request: NextRequest) {
  const session = await auth()
  if (!session) {
    return NextResponse.json({ error: "No autorizado" }, { status: 401 })
  }

  const id = request.nextUrl.searchParams.get("id")
  if (!id || !mongoose.isValidObjectId(id)) {
    return NextResponse.json({ error: "ID inválido" }, { status: 400 })
  }

  await connectDB()
  const deleted = await Caso.findByIdAndDelete(id)
  if (!deleted) {
    return NextResponse.json({ error: "No encontrado" }, { status: 404 })
  }
  return NextResponse.json({ ok: true })
}
```

Notas de seguridad implementadas:
- Cada handler llama `auth()` PRIMERO, antes de tocar la DB (ASVS V4)
- DELETE valida `mongoose.isValidObjectId(id)` antes de `findByIdAndDelete` (evita excepción de Mongoose con ids malformados)
- POST usa `casoSchema.safeParse()` — nunca se pasan datos crudos a `Caso.create()`
- `force-dynamic` previene caché estático de Next.js en GET
  </action>
  <verify>
    <automated>cd tma && npm run test:ci -- --reporter=verbose src/__tests__/casos/casosRoute.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tma/src/app/api/casos/route.ts existe
    - route.ts contiene "export const dynamic = \"force-dynamic\""
    - route.ts contiene "export async function GET()"
    - route.ts contiene "export async function POST(request: NextRequest)"
    - route.ts contiene "export async function DELETE(request: NextRequest)"
    - route.ts contiene "mongoose.isValidObjectId(id)" en DELETE
    - route.ts contiene "casoSchema.safeParse(body)" en POST
    - route.ts contiene "T12:00:00" en las conversiones de fecha (pitfall de timezone)
    - Cada uno de los 3 handlers tiene "if (!session)" retornando 401 como primera comprobación
    - npm run test:ci -- src/__tests__/casos/casosRoute.test.ts sale con código 0 (GREEN)
  </acceptance_criteria>
  <done>Route Handler completo con GET/POST/DELETE. Seguridad ASVS: auth() primero, isValidObjectId en DELETE, safeParse en POST. Tests casosRoute.test.ts pasando GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/casos | Requests HTTP no autenticados o con sesión expirada cruzan esta frontera |
| /api/casos → MongoDB | IDs y datos no validados podrían llegar a la DB si no se filtra en el handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Elevation of Privilege | GET/POST/DELETE /api/casos | mitigate | `auth()` como primer statement en cada handler — retorna 401 antes de cualquier operación DB |
| T-4-02 | Tampering | POST /api/casos body | mitigate | `casoSchema.safeParse(body)` — datos inválidos o extras son rechazados; el objeto pasado a `Caso.create()` es solo `result.data` |
| T-4-03 | Tampering | DELETE /api/casos?id= | mitigate | `mongoose.isValidObjectId(id)` antes de `findByIdAndDelete` — previene excepción con IDs malformados y potential DoS |
| T-4-04 | Information Disclosure | Error responses | accept | Los mensajes de error (401, 400, 404) son genéricos; no exponen stack traces ni detalles de DB. App single-tenant — baja sensibilidad |
</threat_model>

<verification>
```bash
cd tma && npm run test:ci -- --reporter=verbose src/__tests__/casos/casoSchema.test.ts src/__tests__/casos/casosRoute.test.ts
```
Ambos suites deben salir con código 0 y todos los tests en verde.
</verification>

<success_criteria>
- Caso.ts: interface ICaso + CasoSchema + guard mongoose.models.Caso
- validations.ts: casoSchema exportado con 4 campos y mensajes en español exactos del UI-SPEC
- route.ts: GET + POST + DELETE con auth() como primer check, dynamic = force-dynamic, isValidObjectId en DELETE, safeParse en POST, conversión de fecha con T12:00:00
- npm run test:ci -- src/__tests__/casos/ sale con código 0 para casoSchema.test.ts y casosRoute.test.ts
</success_criteria>

<output>
Luego de completar, crear `.planning/phases/04-casos-crud-dashboard/04-02-SUMMARY.md` con:
- Archivos creados/modificados
- Schema fields del modelo Caso
- Endpoints implementados con sus status codes
- Tests en GREEN
</output>
