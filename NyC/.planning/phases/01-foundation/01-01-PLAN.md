---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - components.json
  - vercel.json
  - .env.example
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/Footer.tsx
  - src/__tests__/components/Footer.test.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "El proyecto Next.js 15 compila sin errores con `npm run build`"
    - "Tailwind v4 aplica la paleta Driva Dev — clases `bg-brand-primary`, `text-brand-title`, etc. disponibles"
    - "Poppins se carga como variable CSS `--font-poppins` y se aplica al body en todas las páginas"
    - "El Footer renderiza 'Desarrollado por Driva Dev' con link a https://drivadev.com.ar en el root layout"
    - "cult-ui TextureCard y TextureButton son instalables via shadcn registry"
    - "vercel.json tiene maxDuration: 60"
    - "Los tests de Footer pasan con assertions reales (no stubs)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Paleta Driva Dev via @theme + @import tailwindcss"
      contains: "@import \"tailwindcss\""
    - path: "src/app/layout.tsx"
      provides: "Root layout con Poppins + Footer"
      exports: ["default RootLayout"]
    - path: "src/components/Footer.tsx"
      provides: "Componente Footer con link a drivadev.com.ar"
      exports: ["Footer"]
    - path: "components.json"
      provides: "Configuración shadcn + cult-ui registry"
      contains: "cult-ui.com/r"
    - path: "vercel.json"
      provides: "maxDuration 60 para route de contratos"
      contains: "maxDuration"
    - path: ".env.example"
      provides: "Variables de entorno documentadas"
      contains: "MONGODB_URI"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/Footer.tsx"
      via: "import e instancia en JSX"
      pattern: "import.*Footer"
    - from: "src/app/globals.css"
      to: "paleta Driva Dev"
      via: "@theme block con --color-brand-* variables"
      pattern: "--color-brand-primary"
    - from: "src/app/layout.tsx"
      to: "Poppins variable"
      via: "className={poppins.variable} en <html>"
      pattern: "poppins.variable"
---

<objective>
Scaffoldear el proyecto Next.js 15 desde cero con todas las dependencias, configurar Tailwind v4 con la paleta Driva Dev, cargar Poppins via next/font/google, crear el root layout con Footer, instalar cult-ui y configurar vercel.json.

Purpose: Establece la base sobre la que se construye toda la app. Sin este plan, no existe proyecto. Define la identidad visual para el resto de las fases.

Output: Proyecto compilable con paleta Driva Dev aplicada, Poppins cargada, Footer en todas las páginas, cult-ui listo para usar, vercel.json configurado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Paleta exacta Driva Dev (D-12, UI-01): -->
<!-- #EA580C = CTAs (brand-primary) -->
<!-- #9A3412 = Titulares H1/H2 (brand-title) -->
<!-- #FED7AA = Acento / superficies de cards (brand-accent) -->
<!-- #FFF7ED = Fondo (brand-background) -->
<!-- #1C1917 = Texto oscuro (brand-text) -->

<!-- Poppins pesos requeridos (D-13, UI-02): 300, 400, 500, 700 -->

<!-- cult-ui registry entry exacto (01-RESEARCH.md Pattern 8): -->
<!-- "registries": { "@cult-ui": "https://cult-ui.com/r/{name}.json" } -->

<!-- Estructura de directorios (01-RESEARCH.md §Recommended Project Structure): -->
<!-- src/ dir con alias @/ apuntando a src/ -->
<!-- auth.ts en src/auth.ts (NO dentro de src/app/) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Scaffold del proyecto y dependencias</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    components.json
    vercel.json
    .env.example
  </files>

  <read_first>
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Standard Stack (versiones exactas), §Instalacion (comandos exactos), §Architecture Patterns Pattern 8 (components.json)
    - `.planning/phases/01-foundation/01-CONTEXT.md` §Decisions D-08, D-15 (stack decisions bloqueadas)
    - `CLAUDE.md` (reglas globales: cult-ui, motion, TypeScript strict)
  </read_first>

  <action>
Ejecutar en el directorio de trabajo (`C:\Users\Equipo\Documents\NyC`):

```bash
# 1. Scaffold con flags exactos (D-15)
npx create-next-app@latest tma --typescript --tailwind --app --src-dir --import-alias "@/*" --no-git

cd tma

# 2. NextAuth v5 — CRÍTICO: instalar como @5, no @latest (latest = v4.x)
npm install next-auth@5

# 3. Base auth + DB
npm install mongoose bcryptjs zod

# 4. Tipos TypeScript
npm install --save-dev @types/bcryptjs

# 5. Motion para animaciones (CLAUDE.md global)
npm install motion

# 6. Inicializar shadcn (responder al prompt: style=new-york, rsc=true, tsx=true)
# Al preguntar por el path de globals.css → especificar: src/app/globals.css
# Al preguntar por aliases → confirmar: @/components, @/lib/utils
npx shadcn@beta init

# 7. Agregar componentes cult-ui y shadcn base
npx shadcn@beta add @cult-ui/texture-card @cult-ui/texture-button
npx shadcn@beta add input label form
```

Después del scaffold, editar manualmente:

**components.json** — agregar el registry de cult-ui en la raíz del JSON:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide",
  "registries": {
    "@cult-ui": "https://cult-ui.com/r/{name}.json"
  }
}
```

**vercel.json** — crear en la raíz del proyecto (`tma/vercel.json`):
```json
{
  "functions": {
    "src/app/api/contracts/generate/route.ts": {
      "maxDuration": 60
    }
  }
}
```

**.env.example** — crear en la raíz del proyecto:
```
# MongoDB Atlas M0 — obtener en cloud.mongodb.com → Connect → Drivers
MONGODB_URI=mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/tma?retryWrites=true&w=majority

# NextAuth v5 — generar con: openssl rand -base64 33
AUTH_SECRET=your-secret-here

# URL de la app (para producción en Vercel)
# AUTH_URL=https://your-app.vercel.app
```

**tsconfig.json** — verificar que `"strict": true` esté en compilerOptions. Si create-next-app no lo incluyó, agregar.
  </action>

  <verify>
    <automated>cd tma && npm run build 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `tma/package.json` contiene `"next-auth": "^5` (no `"4`)
    - `tma/package.json` contiene `"mongoose"`, `"bcryptjs"`, `"zod"`, `"motion"`
    - `tma/components.json` contiene la string `"cult-ui.com/r/{name}.json"`
    - `tma/vercel.json` contiene `"maxDuration": 60`
    - `tma/.env.example` contiene `MONGODB_URI=` y `AUTH_SECRET=`
    - `tma/tsconfig.json` contiene `"strict": true`
    - `tma/src/components/ui/` contiene `texture-card.tsx` y `texture-button.tsx` (instalados por shadcn)
    - `npm run build` en directorio `tma/` sale con código 0 (sin errores de compilación)
  </acceptance_criteria>

  <done>Proyecto compilable con todas las dependencias instaladas, components.json con cult-ui registry, vercel.json con maxDuration: 60, .env.example documentado.</done>
</task>

<task type="auto">
  <name>Tarea 2: Tailwind v4 paleta Driva Dev + Poppins + Footer + Root Layout</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
    src/components/Footer.tsx
  </files>

  <read_first>
    - `tma/src/app/globals.css` — ver estado actual generado por create-next-app (para reemplazar correctamente)
    - `tma/src/app/layout.tsx` — ver estado actual generado por create-next-app
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Pattern 6 (globals.css con @theme), Pattern 7 (Poppins), §Code Examples §Footer component
    - `.planning/phases/01-foundation/01-UI-SPEC.md` §Design System, §Color, §Typography, §Page Specifications §Global layout
  </read_first>

  <action>
**`tma/src/app/globals.css`** — REEMPLAZAR completamente el contenido generado por create-next-app:

```css
@import "tailwindcss";

@theme {
  /* Paleta Driva Dev (UI-01, D-12) */
  --color-brand-primary: #EA580C;
  --color-brand-title: #9A3412;
  --color-brand-accent: #FED7AA;
  --color-brand-background: #FFF7ED;
  --color-brand-text: #1C1917;

  /* Tipografía Poppins (UI-02, D-13) */
  --font-poppins: var(--font-poppins);
}

body {
  background-color: var(--color-brand-background);
  color: var(--color-brand-text);
  font-family: var(--font-poppins), sans-serif;
}
```

IMPORTANTE: En Tailwind v4, NO usar `@tailwind base; @tailwind components; @tailwind utilities`. Solo `@import "tailwindcss"`. NO usar tailwind.config.ts para colores — van en `@theme {}` aquí.

---

**`tma/src/components/Footer.tsx`** — crear (directorio `src/components/` puede no existir, crearlo):

```tsx
export function Footer() {
  return (
    <footer className="w-full py-3 text-center" style={{ height: "48px", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <p className="text-[11px] font-normal text-brand-text/60">
        Desarrollado por{" "}
        <a
          href="https://drivadev.com.ar"
          target="_blank"
          rel="noopener noreferrer"
          className="text-brand-primary hover:text-brand-title font-medium underline-offset-2 hover:underline transition-colors"
        >
          Driva Dev
        </a>
      </p>
    </footer>
  )
}
```

---

**`tma/src/app/layout.tsx`** — REEMPLAZAR con:

```tsx
import type { Metadata } from "next"
import { Poppins } from "next/font/google"
import { Footer } from "@/components/Footer"
import "./globals.css"

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["300", "400", "500", "700"],
  variable: "--font-poppins",
  display: "swap",
})

export const metadata: Metadata = {
  title: "TMA — Nicholson & Cano",
  description: "App interna de gestión de asuntos y contratos",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="es" className={poppins.variable}>
      <body className="min-h-screen flex flex-col bg-brand-background text-brand-text antialiased">
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
```

NOTA: `main` envuelve el contenido y ocupa el espacio disponible (flex-1). El Footer queda al fondo naturalmente con flex flex-col.
  </action>

  <verify>
    <automated>cd tma && npm run build 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `tma/src/app/globals.css` primera línea es exactamente `@import "tailwindcss";`
    - `tma/src/app/globals.css` contiene `--color-brand-primary: #EA580C`
    - `tma/src/app/globals.css` contiene `--color-brand-background: #FFF7ED`
    - `tma/src/app/globals.css` NO contiene `@tailwind base` ni `@tailwind components` ni `@tailwind utilities`
    - `tma/src/components/Footer.tsx` contiene `href="https://drivadev.com.ar"`
    - `tma/src/components/Footer.tsx` contiene el texto `Desarrollado por`
    - `tma/src/app/layout.tsx` contiene `import { Poppins } from "next/font/google"`
    - `tma/src/app/layout.tsx` contiene `import { Footer } from "@/components/Footer"`
    - `tma/src/app/layout.tsx` contiene `className={poppins.variable}` en el elemento `<html>`
    - `npm run build` sale con código 0
  </acceptance_criteria>

  <done>globals.css con paleta Driva Dev via @theme, Poppins cargada como CSS variable en root layout, Footer renderizado en todas las páginas via layout.tsx. El build compila sin errores.</done>
</task>

<task type="auto">
  <name>Tarea 3: Implementar assertions reales en Footer.test.tsx (UI-03)</name>
  <files>
    src/__tests__/components/Footer.test.tsx
  </files>

  <read_first>
    - `tma/src/__tests__/components/Footer.test.tsx` — ver el stub creado por el Plan 00 (contiene TODOs comentados)
    - `tma/src/components/Footer.tsx` — ver el componente Footer recién creado en la Tarea 2 de este plan
  </read_first>

  <action>
El Plan 00 creó `src/__tests__/components/Footer.test.tsx` con stubs `expect(true).toBe(true)`. Ahora que `Footer.tsx` existe, reemplazar el contenido completo del archivo con assertions reales:

```typescript
import { describe, it, expect } from "vitest"
import { render, screen } from "@testing-library/react"
import "@testing-library/jest-dom"
import { Footer } from "@/components/Footer"

describe("Footer — UI-03", () => {
  it("UI-03: renderiza el texto 'Desarrollado por'", () => {
    render(<Footer />)
    expect(screen.getByText(/Desarrollado por/i)).toBeInTheDocument()
  })

  it("UI-03: contiene link con texto 'Driva Dev' que apunta a https://drivadev.com.ar", () => {
    render(<Footer />)
    const link = screen.getByRole("link", { name: /Driva Dev/i })
    expect(link).toBeInTheDocument()
    expect(link).toHaveAttribute("href", "https://drivadev.com.ar")
  })

  it("UI-03: el link tiene target='_blank' y rel='noopener noreferrer'", () => {
    render(<Footer />)
    const link = screen.getByRole("link", { name: /Driva Dev/i })
    expect(link).toHaveAttribute("target", "_blank")
    expect(link).toHaveAttribute("rel", "noopener noreferrer")
  })
})
```

NOTA: `@testing-library/jest-dom` provee los matchers `toBeInTheDocument()` y `toHaveAttribute()`. El import debe agregarse al inicio del archivo de test — si la suite falla por matchers no reconocidos, agregar al `setupFiles` de `vitest.config.ts`:
```typescript
setupFiles: ["./src/__tests__/setup.ts"]
```
y crear `src/__tests__/setup.ts` con:
```typescript
import "@testing-library/jest-dom"
```
  </action>

  <verify>
    <automated>cd tma && npm test -- --run --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `tma/src/__tests__/components/Footer.test.tsx` NO contiene `expect(true).toBe(true)` (stubs eliminados)
    - `tma/src/__tests__/components/Footer.test.tsx` contiene `import { Footer } from "@/components/Footer"` (import real)
    - `tma/src/__tests__/components/Footer.test.tsx` contiene `screen.getByRole("link", { name: /Driva Dev/i })` (assertion real)
    - `npm test -- --run` sale con código 0
    - La salida del test reporter muestra "3 passed" para el describe block "Footer — UI-03" (no 0 assertions)
  </acceptance_criteria>

  <done>Footer.test.tsx implementado con 3 assertions reales: texto visible, link href, y atributos de seguridad. Los tests pasan en verde importando el Footer real — cobertura real de UI-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Next.js build | Archivos de config leídos en build-time; un globals.css malformado rompe el build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example | accept | .env.example documenta variables sin valores reales — los secretos van en .env.local (gitignored) y Vercel Dashboard. El archivo example en sí no es un riesgo. |
| T-01-02 | Tampering | vercel.json maxDuration | accept | maxDuration: 60 solo aplica a la route de generación de contratos. No hay riesgo en esta fase — la route aún no existe. |
| T-01-03 | Information Disclosure | AUTH_SECRET en código | mitigate | AUTH_SECRET definido SOLO en .env.example como placeholder — nunca con valor real en código. En producción, usar Vercel environment variables. El .gitignore de create-next-app ya excluye .env.local. |
</threat_model>

<verification>
```bash
# Desde el directorio tma/
cd tma

# 1. Build limpio
npm run build

# 2. Verificar dependencias críticas
node -e "const p = require('./package.json'); console.log('next-auth:', p.dependencies['next-auth']); console.log('mongoose:', p.dependencies['mongoose']); console.log('bcryptjs:', p.dependencies['bcryptjs'])"

# 3. Verificar globals.css
grep -c "@import \"tailwindcss\"" src/app/globals.css
grep -c "brand-primary" src/app/globals.css

# 4. Verificar Footer en layout
grep -c "Footer" src/app/layout.tsx

# 5. Verificar cult-ui registry
grep -c "cult-ui.com" components.json

# 6. Verificar vercel.json
grep -c "maxDuration" vercel.json

# 7. Tests del Footer con assertions reales
npm test -- --run --reporter=verbose
# Espera: "3 passed" en describe "Footer — UI-03"
```
</verification>

<success_criteria>
- `npm run build` sale con código 0 sin warnings de TypeScript
- `package.json` contiene `next-auth` con versión `^5`, `mongoose`, `bcryptjs`, `zod`, `motion`
- `globals.css` usa `@import "tailwindcss"` y define los 5 tokens de paleta Driva Dev en `@theme {}`
- `layout.tsx` aplica Poppins via `poppins.variable` en `<html>` e incluye `<Footer />`
- `components.json` tiene el registro `@cult-ui` apuntando a `cult-ui.com/r/{name}.json`
- `vercel.json` define `maxDuration: 60`
- `src/components/ui/texture-card.tsx` y `texture-button.tsx` existen (instalados por CLI)
- `npm test -- --run` pasa con assertions reales en Footer.test.tsx (no stubs)
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/01-foundation/01-01-SUMMARY.md` siguiendo el template en `@$HOME/.claude/get-shit-done/templates/summary.md`.

Incluir en el summary:
- Decisiones tomadas durante scaffold (versiones instaladas, respuestas a prompts de shadcn)
- Cualquier diferencia entre lo planeado y lo ejecutado (ej: si cult-ui requirió ajuste por Tailwind v4)
- Path absoluto del directorio del proyecto: `C:\Users\Equipo\Documents\NyC\tma\`
</output>
