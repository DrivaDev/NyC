---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
files_modified:
  - src/app/page.tsx
  - src/app/login/page.tsx
  - src/app/register/page.tsx
  - src/app/tma/page.tsx
  - src/actions/auth.login.ts
  - src/components/auth/LoginForm.tsx
  - src/components/auth/RegisterForm.tsx
  - src/components/TmaPageContent.tsx
  - src/__tests__/actions/auth.login.test.ts
autonomous: true
requirements:
  - UI-07
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Usuario puede ver /login con formulario de email y contraseña, H1 'Iniciar sesión' en #9A3412, y TextureButton para submit"
    - "Usuario puede ver /register con formulario, H1 'Crear cuenta' en #9A3412, y mensajes de error inline en español"
    - "Credenciales inválidas en /login muestran 'Email o contraseña incorrectos' inline sin recargar la página"
    - "Email fuera de allowlist en /register muestra 'Este email no está autorizado' inline"
    - "Email duplicado en /register muestra 'Este email ya tiene cuenta, iniciá sesión' con link a /login"
    - "Formularios tienen animación de entrada con motion: opacity 0→1, y 0→16 en 0.35s"
    - "/tma muestra 'Bienvenido, NyC' con 2 TextureCards deshabilitadas (opacity-40, cursor-not-allowed)"
    - "El Footer aparece en /login, /register y /tma (heredado del root layout del Plan 01)"
    - "/ no tiene página propia — el middleware del Plan 02 gestiona el redirect"
    - "Los tests de loginAction pasan con assertions reales (AUTH-03)"
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "Página de login (Server Component wrapper)"
      exports: ["default LoginPage"]
    - path: "src/app/register/page.tsx"
      provides: "Página de registro (Server Component wrapper)"
      exports: ["default RegisterPage"]
    - path: "src/app/tma/page.tsx"
      provides: "Server Component async que verifica sesión y renderiza TmaPageContent"
      exports: ["default TmaPage"]
    - path: "src/components/TmaPageContent.tsx"
      provides: "Client Component con motion animations para /tma"
      contains: "use client"
    - path: "src/actions/auth.login.ts"
      provides: "Server Action de login — separada del Client Component"
      contains: "use server"
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Client Component con useActionState para login"
      contains: "use client"
    - path: "src/components/auth/RegisterForm.tsx"
      provides: "Client Component con useActionState para registro"
      contains: "use client"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "src/components/auth/LoginForm.tsx"
      via: "import e instancia en JSX"
      pattern: "LoginForm"
    - from: "src/app/register/page.tsx"
      to: "src/components/auth/RegisterForm.tsx"
      via: "import e instancia en JSX"
      pattern: "RegisterForm"
    - from: "src/app/tma/page.tsx"
      to: "src/components/TmaPageContent.tsx"
      via: "import e instancia en JSX"
      pattern: "TmaPageContent"
    - from: "src/components/auth/RegisterForm.tsx"
      to: "src/actions/auth.register.ts"
      via: "useActionState(registerUser, undefined)"
      pattern: "registerUser"
    - from: "src/components/auth/LoginForm.tsx"
      to: "src/actions/auth.login.ts"
      via: "useActionState(loginAction, undefined)"
      pattern: "loginAction"
---

<objective>
Implementar todas las páginas de la interfaz de usuario: /login y /register con formularios Client Components usando useActionState, animaciones motion, y mensajes de error inline; /tma como placeholder con branding completo y cards deshabilitadas usando el patron Server Component + Client Component; y src/app/page.tsx vacío (el redirect lo maneja el middleware).

Purpose: Completa la experiencia de usuario end-to-end. Sin estas páginas, el sistema de auth existe pero no hay forma de interactuar con él.

Output: Tres páginas funcionales con branding Driva Dev aplicado — paleta naranja/crema, Poppins, Footer — y flujo completo de registro/login verificable en el navegador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<!-- PRECONDICIÓN: Este plan depende de los planes 01 y 02. Los archivos
     01-01-SUMMARY.md y 01-02-SUMMARY.md solo existen después de ejecutar
     esos planes. Si no existen al iniciar este plan, es un error de
     precondición — verificar que los planes anteriores se hayan completado
     antes de continuar. -->
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Server Action disponible (Plan 02 — src/actions/auth.register.ts): -->
<!-- import { registerUser } from "@/actions/auth.register" -->
<!-- Signature: registerUser(_: unknown, formData: FormData): Promise<{ error?: string } | void> -->

<!-- Server Action de login (Plan 03 — ESTE PLAN crea src/actions/auth.login.ts): -->
<!-- import { loginAction } from "@/actions/auth.login" -->
<!-- Signature: loginAction(_: unknown, formData: FormData): Promise<{ error?: string } | undefined> -->
<!-- IMPORTANTE: loginAction vive en un archivo "use server" SEPARADO del Client Component -->
<!-- Next.js 15 App Router NO soporta Server Actions inline dentro de archivos "use client" -->

<!-- NextAuth signIn (Plan 02 — src/auth.ts): -->
<!-- import { signIn } from "@/auth" — solo usar en archivos "use server", NO en "use client" -->

<!-- Allowlist correcta (D-09, CLAUDE.md): -->
<!-- nsilva@nyc.com.ar, crivera@nyc.com.ar, tderosa@nyc.com.ar, vespinola@nyc.com.ar, ekoch@nyc.com.ar -->
<!-- NOTA: el 5to email es vespinola (no vespinosa) — ya corregido en auth.register.ts del Plan 02 -->

<!-- TextureCard y TextureButton (Plan 01 — instalados via shadcn): -->
<!-- import { TextureCard } from "@/components/ui/texture-card" -->
<!-- import { TextureButton } from "@/components/ui/texture-button" -->
<!-- Leer src/components/ui/texture-card.tsx para ver props disponibles -->
<!-- Leer src/components/ui/texture-button.tsx para ver variant names -->

<!-- Clases Tailwind v4 disponibles (Plan 01 — globals.css @theme): -->
<!-- bg-brand-background, bg-brand-primary, bg-brand-accent -->
<!-- text-brand-title, text-brand-text, text-brand-primary -->

<!-- shadcn Input y Label (Plan 01 — instalados): -->
<!-- import { Input } from "@/components/ui/input" -->
<!-- import { Label } from "@/components/ui/label" -->

<!-- motion import (Plan 01 — instalado): -->
<!-- import { motion } from "motion/react" (NO "framer-motion") -->

<!-- Tipografía exacta (UI-SPEC §Typography): -->
<!-- H1: text-[28px] font-bold text-brand-title -->
<!-- H2: text-[20px] font-bold text-brand-title -->
<!-- Body: text-[14px] font-normal text-brand-text -->
<!-- Caption: text-[11px] font-normal text-brand-text -->

<!-- Copy exacto (UI-SPEC §Copywriting Contract): -->
<!-- /login H1: "Iniciar sesión" -->
<!-- /register H1: "Crear cuenta" -->
<!-- /tma H1: "Bienvenido, NyC" -->
<!-- Error credenciales: "Email o contraseña incorrectos" -->
<!-- Error allowlist: "Este email no está autorizado" -->
<!-- Error duplicado: "Este email ya tiene cuenta, iniciá sesión" (link en "iniciá sesión") -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Server Action de login + Formularios de auth (LoginForm, RegisterForm) + páginas server wrapper</name>
  <files>
    src/actions/auth.login.ts
    src/components/auth/LoginForm.tsx
    src/components/auth/RegisterForm.tsx
    src/app/login/page.tsx
    src/app/register/page.tsx
    src/app/page.tsx
  </files>

  <read_first>
    - `tma/src/components/ui/texture-card.tsx` — ver props disponibles (className, children, etc.)
    - `tma/src/components/ui/texture-button.tsx` — ver props disponibles (variant, etc.) y nombre exacto del variant para color primario
    - `tma/src/components/ui/input.tsx` — confirmar que existe y su API
    - `tma/src/components/ui/label.tsx` — confirmar que existe y su API
    - `.planning/phases/01-foundation/01-UI-SPEC.md` §Page Specifications §/login, §/register, §Copywriting Contract
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Architecture Patterns §System Architecture Diagram (flujo form → Server Action)
    - `.planning/phases/01-foundation/01-RESEARCH.md` §Common Pitfalls Pitfall 5 (signIn re-lanza NEXT_REDIRECT)
  </read_first>

  <action>
PASO 0 — Crear el Server Action de login en archivo separado.

En Next.js 15 App Router, las Server Actions NO pueden definirse inline dentro de un archivo "use client". Deben vivir en archivos propios con directiva "use server" al tope.

**`tma/src/actions/auth.login.ts`** — crear este archivo ANTES de crear LoginForm.tsx:
```typescript
"use server"

import { signIn } from "@/auth"
import { AuthError } from "next-auth"

export async function loginAction(
  _: { error?: string } | undefined,
  formData: FormData
): Promise<{ error?: string } | undefined> {
  try {
    await signIn("credentials", {
      email: formData.get("email") as string,
      password: formData.get("password") as string,
      redirectTo: "/tma",
    })
  } catch (error) {
    if (error instanceof AuthError) {
      return { error: "Email o contraseña incorrectos" }
    }
    throw error // Re-lanzar NEXT_REDIRECT para que Next.js procese el redirect
  }
}
```

---

PASO 1 — Crear el directorio `tma/src/components/auth/` y los formularios:

**`tma/src/components/auth/LoginForm.tsx`** — Client Component (importa loginAction del archivo separado):
```tsx
"use client"

import { useActionState } from "react"
import { loginAction } from "@/actions/auth.login"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { TextureButton } from "@/components/ui/texture-button"
import { motion } from "motion/react"
import Link from "next/link"

export function LoginForm() {
  const [state, action, isPending] = useActionState(loginAction, undefined)

  return (
    <motion.div
      initial={{ opacity: 0, y: 16 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.35, ease: "easeOut" }}
      className="w-full max-w-[400px] mx-auto px-4"
    >
      <h1 className="text-[28px] font-bold text-brand-title mb-8 text-center">
        Iniciar sesión
      </h1>

      <form action={action} className="flex flex-col gap-5">
        <div className="flex flex-col gap-1.5">
          <Label htmlFor="email" className="text-[14px] font-normal text-brand-text">
            Email
          </Label>
          <Input
            id="email"
            name="email"
            type="email"
            placeholder="tu@nyc.com.ar"
            required
            autoComplete="email"
            className="bg-white border-brand-accent focus-visible:ring-brand-primary"
          />
        </div>

        <div className="flex flex-col gap-1.5">
          <Label htmlFor="password" className="text-[14px] font-normal text-brand-text">
            Contraseña
          </Label>
          <Input
            id="password"
            name="password"
            type="password"
            placeholder="••••••••"
            required
            autoComplete="current-password"
            className="bg-white border-brand-accent focus-visible:ring-brand-primary"
          />
        </div>

        {state?.error && (
          <p className="text-[11px] text-red-600" role="alert">
            {state.error}
          </p>
        )}

        <TextureButton
          type="submit"
          disabled={isPending}
          className="w-full bg-brand-primary hover:bg-brand-title text-white font-medium h-11"
        >
          {isPending ? "Ingresando..." : "Iniciar sesión"}
        </TextureButton>
      </form>

      <p className="text-[11px] text-brand-text text-center mt-6">
        ¿No tenés cuenta?{" "}
        <Link href="/register" className="text-brand-primary hover:text-brand-title transition-colors">
          Registrate
        </Link>
      </p>
    </motion.div>
  )
}
```

NOTA: Si TextureButton no acepta las clases de override via `className`, leer `src/components/ui/texture-button.tsx` para ver cómo aplicar el color primario (#EA580C). Ajustar según la API real del componente.

---

**`tma/src/components/auth/RegisterForm.tsx`** — Client Component:
```tsx
"use client"

import { useActionState } from "react"
import { registerUser } from "@/actions/auth.register"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { TextureButton } from "@/components/ui/texture-button"
import { motion } from "motion/react"
import Link from "next/link"

export function RegisterForm() {
  const [state, action, isPending] = useActionState(registerUser, undefined)

  return (
    <motion.div
      initial={{ opacity: 0, y: 16 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.35, ease: "easeOut" }}
      className="w-full max-w-[400px] mx-auto px-4"
    >
      <h1 className="text-[28px] font-bold text-brand-title mb-8 text-center">
        Crear cuenta
      </h1>

      <form action={action} className="flex flex-col gap-5">
        <div className="flex flex-col gap-1.5">
          <Label htmlFor="email" className="text-[14px] font-normal text-brand-text">
            Email
          </Label>
          <Input
            id="email"
            name="email"
            type="email"
            placeholder="tu@nyc.com.ar"
            required
            autoComplete="email"
            className="bg-white border-brand-accent focus-visible:ring-brand-primary"
          />
        </div>

        <div className="flex flex-col gap-1.5">
          <Label htmlFor="password" className="text-[14px] font-normal text-brand-text">
            Contraseña
          </Label>
          <Input
            id="password"
            name="password"
            type="password"
            placeholder="Mínimo 8 caracteres"
            required
            autoComplete="new-password"
            className="bg-white border-brand-accent focus-visible:ring-brand-primary"
          />
        </div>

        {state?.error && (
          <p className="text-[11px] text-red-600" role="alert">
            {state.error === "Este email ya tiene cuenta, iniciá sesión" ? (
              <>
                Este email ya tiene cuenta,{" "}
                <Link href="/login" className="text-brand-primary hover:text-brand-title underline">
                  iniciá sesión
                </Link>
              </>
            ) : (
              state.error
            )}
          </p>
        )}

        <TextureButton
          type="submit"
          disabled={isPending}
          className="w-full bg-brand-primary hover:bg-brand-title text-white font-medium h-11"
        >
          {isPending ? "Creando cuenta..." : "Crear cuenta"}
        </TextureButton>
      </form>

      <p className="text-[11px] text-brand-text text-center mt-6">
        ¿Ya tenés cuenta?{" "}
        <Link href="/login" className="text-brand-primary hover:text-brand-title transition-colors">
          Iniciá sesión
        </Link>
      </p>
    </motion.div>
  )
}
```

---

**`tma/src/app/login/page.tsx`** — Server Component wrapper:
```tsx
import { LoginForm } from "@/components/auth/LoginForm"

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-brand-background py-16 px-4">
      <LoginForm />
    </div>
  )
}
```

---

**`tma/src/app/register/page.tsx`** — Server Component wrapper:
```tsx
import { RegisterForm } from "@/components/auth/RegisterForm"

export default function RegisterPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-brand-background py-16 px-4">
      <RegisterForm />
    </div>
  )
}
```

---

**`tma/src/app/page.tsx`** — Reemplazar con página vacía (el middleware maneja el redirect de /):
```tsx
// El middleware en src/middleware.ts gestiona el redirect de /
// → /tma si hay sesión activa
// → /login si no hay sesión
// Esta página nunca se renderiza en condiciones normales.
export default function RootPage() {
  return null
}
```
  </action>

  <verify>
    <automated>cd tma && npm run build 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `tma/src/actions/auth.login.ts` primera línea es `"use server"` (Server Action en archivo separado)
    - `tma/src/actions/auth.login.ts` contiene `import { signIn } from "@/auth"`
    - `tma/src/actions/auth.login.ts` contiene `"Email o contraseña incorrectos"` (copy exacto UI-SPEC)
    - `tma/src/actions/auth.login.ts` contiene `if (error instanceof AuthError)` seguido de `throw error` (Pitfall 5)
    - `tma/src/components/auth/LoginForm.tsx` primera línea es `"use client"`
    - `tma/src/components/auth/LoginForm.tsx` contiene `import { loginAction } from "@/actions/auth.login"` (NO signIn directo desde @/auth)
    - `tma/src/components/auth/LoginForm.tsx` contiene `useActionState(loginAction`
    - `tma/src/components/auth/LoginForm.tsx` contiene `from "motion/react"` (NO `from "framer-motion"`)
    - `tma/src/components/auth/LoginForm.tsx` NO contiene `import { signIn } from "@/auth"` (signIn solo en "use server")
    - `tma/src/components/auth/RegisterForm.tsx` primera línea es `"use client"`
    - `tma/src/components/auth/RegisterForm.tsx` contiene `import { registerUser } from "@/actions/auth.register"`
    - `tma/src/components/auth/RegisterForm.tsx` contiene `useActionState(registerUser`
    - `tma/src/components/auth/RegisterForm.tsx` contiene link a `/login` en el mensaje de error de email duplicado
    - `tma/src/app/login/page.tsx` contiene `import { LoginForm }` y renderiza `<LoginForm />`
    - `tma/src/app/register/page.tsx` contiene `import { RegisterForm }` y renderiza `<RegisterForm />`
    - `npm run build` sale con código 0
  </acceptance_criteria>

  <done>loginAction en src/actions/auth.login.ts (archivo "use server" separado — compatible con Next.js 15 App Router). LoginForm y RegisterForm como Client Components con useActionState, motion entrance animation, mensajes de error en español. Páginas server wrapper para /login y /register.</done>
</task>

<task type="auto">
  <name>Tarea 2: Página /tma — Server Component + TmaPageContent Client Component</name>
  <files>
    src/app/tma/page.tsx
    src/components/TmaPageContent.tsx
  </files>

  <read_first>
    - `tma/src/components/ui/texture-card.tsx` — ver API completa del componente: props, cómo se aplica disabled state
    - `.planning/phases/01-foundation/01-UI-SPEC.md` §Page Specifications §/tma (placeholder): layout, cards, estados disabled, copy exacto
    - `.planning/phases/01-foundation/01-CONTEXT.md` §Decisions D-07 (saludo "Bienvenido, NyC", 2 cards visibles pero deshabilitadas)
  </read_first>

  <action>
La página /tma usa el patron Server Component + Client Component: `page.tsx` es el Server Component async que verifica la sesión, y `TmaPageContent.tsx` es el Client Component con las animations de `motion/react`. Este patron evita el error "motion en Server Component" y es la arquitectura correcta para Next.js 15.

**`tma/src/components/TmaPageContent.tsx`** — crear PRIMERO (Client Component con motion):
```tsx
"use client"

import { TextureCard } from "@/components/ui/texture-card"
import { motion } from "motion/react"

export function TmaPageContent() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-brand-background py-16 px-4">
      <div className="w-full max-w-[640px]">
        <motion.div
          initial={{ opacity: 0, y: 16 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.35, ease: "easeOut" }}
          className="text-center mb-10"
        >
          <h1 className="text-[28px] font-bold text-brand-title mb-2">
            Bienvenido, NyC
          </h1>
          <p className="text-[14px] font-normal text-brand-text">
            Seleccioná un módulo para comenzar
          </p>
        </motion.div>

        <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
          {/* Card 1: Casos TMA — DISABLED (D-07) */}
          <motion.div
            initial={{ opacity: 0, scale: 0.97 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.35, ease: "easeOut", delay: 0.1 }}
            className="opacity-40 cursor-not-allowed pointer-events-none select-none"
            aria-disabled="true"
          >
            <TextureCard className="h-full p-6">
              <h2 className="text-[20px] font-bold text-brand-title mb-2">
                Casos TMA
              </h2>
              <p className="text-[14px] font-normal text-brand-text">
                Gestión de asuntos
              </p>
              <p className="text-[11px] font-normal text-brand-text mt-4">
                Disponible próximamente
              </p>
            </TextureCard>
          </motion.div>

          {/* Card 2: Contratos TMA — DISABLED (D-07) */}
          <motion.div
            initial={{ opacity: 0, scale: 0.97 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.35, ease: "easeOut", delay: 0.2 }}
            className="opacity-40 cursor-not-allowed pointer-events-none select-none"
            aria-disabled="true"
          >
            <TextureCard className="h-full p-6">
              <h2 className="text-[20px] font-bold text-brand-title mb-2">
                Contratos TMA
              </h2>
              <p className="text-[14px] font-normal text-brand-text">
                Generación de contratos
              </p>
              <p className="text-[11px] font-normal text-brand-text mt-4">
                Disponible próximamente
              </p>
            </TextureCard>
          </motion.div>
        </div>
      </div>
    </div>
  )
}
```

---

**`tma/src/app/tma/page.tsx`** — Server Component async (sin motion — solo verifica sesión y delega a TmaPageContent):
```tsx
import { auth } from "@/auth"
import { redirect } from "next/navigation"
import { TmaPageContent } from "@/components/TmaPageContent"

export default async function TmaPage() {
  // Doble verificación de sesión a nivel página (el middleware ya redirige,
  // pero es buena práctica verificar también en el Server Component)
  const session = await auth()
  if (!session) redirect("/login")

  return <TmaPageContent />
}
```

NOTA: Este patron (Server Component → Client Component) es la arquitectura canónica de Next.js 15 para páginas que necesitan verificación de sesión server-side + animaciones client-side. El Server Component no tiene `motion` — toda la lógica de animación está en TmaPageContent.tsx con `"use client"`.

NOTA sobre TextureCard API: Leer `src/components/ui/texture-card.tsx` para verificar que acepta `className` y `children`. Si la API es diferente, ajustar TmaPageContent.tsx según las props reales.
  </action>

  <verify>
    <automated>cd tma && npm run build 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `tma/src/components/TmaPageContent.tsx` primera línea es `"use client"`
    - `tma/src/components/TmaPageContent.tsx` contiene `import { motion } from "motion/react"` (NO framer-motion)
    - `tma/src/components/TmaPageContent.tsx` contiene `"Bienvenido, NyC"` (copy exacto D-07, UI-SPEC)
    - `tma/src/components/TmaPageContent.tsx` contiene `"Seleccioná un módulo para comenzar"` (copy exacto UI-SPEC)
    - `tma/src/components/TmaPageContent.tsx` contiene `"Casos TMA"` y `"Contratos TMA"` como headings H2
    - `tma/src/components/TmaPageContent.tsx` contiene `"Disponible próximamente"` en ambas cards
    - `tma/src/components/TmaPageContent.tsx` contiene `opacity-40` y `cursor-not-allowed` y `pointer-events-none` (estado disabled UI-SPEC)
    - `tma/src/app/tma/page.tsx` contiene `export default async function TmaPage()`
    - `tma/src/app/tma/page.tsx` contiene `import { auth } from "@/auth"`
    - `tma/src/app/tma/page.tsx` contiene `redirect("/login")` como fallback de sesión
    - `tma/src/app/tma/page.tsx` contiene `import { TmaPageContent }` y renderiza `<TmaPageContent />`
    - `tma/src/app/tma/page.tsx` NO contiene `import { motion }` (motion vive en TmaPageContent, no en el Server Component)
    - `npm run build` sale con código 0
  </acceptance_criteria>

  <done>TmaPageContent.tsx como Client Component "use client" con motion animations y TextureCards deshabilitadas. page.tsx como Server Component async que verifica auth() y delega todo el rendering a TmaPageContent. Patron correcto para Next.js 15 — sin errores de "motion en Server Component".</done>
</task>

<task type="auto">
  <name>Tarea 3: Implementar assertions reales en auth.login.test.ts (AUTH-03)</name>
  <files>
    src/__tests__/actions/auth.login.test.ts
  </files>

  <read_first>
    - `tma/src/__tests__/actions/auth.login.test.ts` — ver el stub creado por Plan 00 (contiene TODOs comentados con el código de test exacto)
    - `tma/src/actions/auth.login.ts` — ver la función loginAction recién creada en la Tarea 1 de este plan
  </read_first>

  <action>
El Plan 00 creó `src/__tests__/actions/auth.login.test.ts` con stubs `expect(true).toBe(true)`. Ahora que `auth.login.ts` existe, reemplazar el contenido completo del archivo con assertions reales:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest"

// Mock de @/auth ANTES del import del módulo bajo test
vi.mock("@/auth", () => ({
  signIn: vi.fn(),
}))

// Import del módulo real — después del mock
import { loginAction } from "@/actions/auth.login"

describe("loginAction — AUTH-03", () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it("AUTH-03: signIn se llama con 'credentials' y redirectTo '/tma' cuando hay credenciales", async () => {
    const { signIn } = await import("@/auth")
    vi.mocked(signIn).mockResolvedValue(undefined as never)
    const formData = new FormData()
    formData.append("email", "nsilva@nyc.com.ar")
    formData.append("password", "password123")
    await loginAction(undefined, formData)
    expect(signIn).toHaveBeenCalledWith("credentials", {
      email: "nsilva@nyc.com.ar",
      password: "password123",
      redirectTo: "/tma",
    })
  })

  it("AUTH-03: retorna { error: 'Email o contraseña incorrectos' } cuando signIn lanza AuthError", async () => {
    const { signIn } = await import("@/auth")
    const { AuthError } = await import("next-auth")
    vi.mocked(signIn).mockRejectedValue(new AuthError("CredentialsSignin"))
    const formData = new FormData()
    formData.append("email", "nsilva@nyc.com.ar")
    formData.append("password", "wrongpass")
    const result = await loginAction(undefined, formData)
    expect(result).toEqual({ error: "Email o contraseña incorrectos" })
  })

  it("AUTH-03: re-lanza NEXT_REDIRECT (no AuthError) para que Next.js procese el redirect", async () => {
    const { signIn } = await import("@/auth")
    // Simular un NEXT_REDIRECT error (no es AuthError — es un error especial de Next.js)
    const nextRedirectError = new Error("NEXT_REDIRECT")
    nextRedirectError.name = "NEXT_REDIRECT"
    vi.mocked(signIn).mockRejectedValue(nextRedirectError)
    const formData = new FormData()
    formData.append("email", "nsilva@nyc.com.ar")
    formData.append("password", "password123")
    // El re-throw debe propagarse — loginAction no debe tragarse el NEXT_REDIRECT
    await expect(loginAction(undefined, formData)).rejects.toThrow("NEXT_REDIRECT")
  })
})
```

NOTA: El test de AuthError requiere que `AuthError` sea importable de `"next-auth"`. Si el mock de `@/auth` interfiere con el import de `next-auth/AuthError`, usar `import("next-auth")` directamente sin mock (el mock solo aplica a `@/auth`, no a `"next-auth"`).
  </action>

  <verify>
    <automated>cd tma && npm test -- --run --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `tma/src/__tests__/actions/auth.login.test.ts` NO contiene `expect(true).toBe(true)` (stubs eliminados)
    - `tma/src/__tests__/actions/auth.login.test.ts` contiene `import { loginAction } from "@/actions/auth.login"` (import real)
    - `tma/src/__tests__/actions/auth.login.test.ts` contiene `expect(signIn).toHaveBeenCalledWith("credentials", ...)` (assertion real AUTH-03)
    - `tma/src/__tests__/actions/auth.login.test.ts` contiene `expect(result).toEqual({ error: "Email o contraseña incorrectos" })`
    - `npm test -- --run` sale con código 0
    - La salida del test reporter muestra "3 passed" para el describe "loginAction — AUTH-03" (no stubs)
  </acceptance_criteria>

  <done>auth.login.test.ts implementado con 3 assertions reales cubriendo AUTH-03: llamada a signIn con los parámetros correctos, retorno de error en AuthError, y re-throw de NEXT_REDIRECT. Los stubs de Plan 00 fueron reemplazados con cobertura funcional real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → LoginForm | FormData con email y password — entrada no confiable |
| Client Component → Server Action | useActionState pasa FormData al Server Action (auth.login.ts o auth.register.ts) |
| Server Component → auth() | TmaPage verifica sesión antes de renderizar TmaPageContent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | TmaPage sin sesión | mitigate | `await auth()` + `redirect("/login")` como segunda línea de defensa detrás del middleware — doble verificación de sesión |
| T-03-02 | Tampering | loginAction Server Action | mitigate | loginAction vive en archivo separado "use server" — Next.js 15 aplica protección CSRF automática; el formulario no acepta inputs arbitrarios fuera de email/password |
| T-03-03 | Information Disclosure | Error messages en formularios | accept | Mensajes de error genéricos en login ("Email o contraseña incorrectos") no revelan si el email existe — consistente con la mitigación de timing en auth.ts |
</threat_model>

<verification>
```bash
# Desde el directorio tma/
cd tma

# 1. Build final limpio
npm run build

# 2. Verificar páginas existen
ls src/app/login/page.tsx src/app/register/page.tsx src/app/tma/page.tsx src/app/page.tsx

# 3. Verificar Server Action de login en archivo separado
grep -c '"use server"' src/actions/auth.login.ts

# 4. Verificar Client Components tienen "use client"
grep -l "use client" src/components/auth/LoginForm.tsx src/components/auth/RegisterForm.tsx src/components/TmaPageContent.tsx

# 5. Verificar que LoginForm NO importa signIn directamente de @/auth
grep -v '^#' src/components/auth/LoginForm.tsx | grep -c '"@/auth"'
# Debe retornar 0

# 6. Verificar motion import correcto (motion/react, no framer-motion)
grep -rn "from \"motion/react\"" src/components/auth/ src/components/TmaPageContent.tsx

# 7. Verificar copy exacto en TmaPageContent
grep -c "Bienvenido, NyC" src/components/TmaPageContent.tsx
grep -c "Disponible próximamente" src/components/TmaPageContent.tsx

# 8. Verificar disabled state en TmaPageContent
grep -c "cursor-not-allowed" src/components/TmaPageContent.tsx

# 9. Verificar que page.tsx de /tma NO tiene motion (motion va en TmaPageContent)
grep -v '^//' src/app/tma/page.tsx | grep -c "motion"
# Debe retornar 0

# 10. Verificar que /tma tiene double-check de sesión
grep -c "await auth()" src/app/tma/page.tsx

# 11. Tests con assertions reales
npm test -- --run --reporter=verbose
# Espera: "3 passed" en describe "loginAction — AUTH-03"

# 12. Dev server smoke test
npm run dev &
sleep 5
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/login
# Espera: 200
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/tma
# Espera: 307 (redirect a /login)
kill %1
```
</verification>

<success_criteria>
- `npm run build` sale con código 0 sin errores de TypeScript
- `src/actions/auth.login.ts` existe como archivo "use server" separado — loginAction no está inline en LoginForm.tsx
- `src/app/login/page.tsx` renderiza `<LoginForm />` con branding Driva Dev aplicado
- `src/app/register/page.tsx` renderiza `<RegisterForm />` con todos los mensajes de error especificados
- `src/app/tma/page.tsx` es async Server Component que delega rendering a `<TmaPageContent />`
- `src/components/TmaPageContent.tsx` tiene `"use client"` con motion + TextureCards en opacity-40 + cursor-not-allowed
- LoginForm y RegisterForm usan `"use client"` + `useActionState` + `motion` desde `motion/react`
- Smoke test: GET /tma sin sesión → 307 redirect a /login
- El Footer aparece en todas las páginas (heredado del root layout del Plan 01)
- `npm test -- --run` pasa con assertions reales en auth.login.test.ts (3 tests, no stubs)
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/01-foundation/01-03-SUMMARY.md` siguiendo el template en `@$HOME/.claude/get-shit-done/templates/summary.md`.

Incluir en el summary:
- Estado final del build
- Cualquier ajuste requerido por la API real de TextureCard/TextureButton (pueden diferir de lo documentado)
- Confirmación de que loginAction vive en src/actions/auth.login.ts (no inline en LoginForm)
- Resultado del smoke test (GET /tma sin sesión → redirect)

Variables de entorno nuevas requeridas para que la app funcione (mostrar al usuario):
- `MONGODB_URI` — connection string de MongoDB Atlas M0
- `AUTH_SECRET` — generar con `openssl rand -base64 33`
</output>
