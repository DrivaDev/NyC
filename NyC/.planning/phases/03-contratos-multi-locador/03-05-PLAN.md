---
phase: 03-contratos-multi-locador
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [CONTR-11, CONTR-12]
must_haves:
  truths:
    - "The full contracts test suite is GREEN (all Phase 3 multi-locador tests pass, no Phase 2 regressions)"
    - "A human has visually confirmed the multi-locador wizard flow and a generated AC .docx with 2 locadores opens in Word without a 'repaired' dialog"
  artifacts: []
  key_links:
    - from: "vitest suite"
      to: "cloneLocadorRow / pluralizeLocadorRefs / route / wizard"
      via: "npm test -- --run contracts"
      pattern: "contracts"
---

<objective>
Wave 3 (verification + acceptance): Confirm the full Phase 3 contracts test suite is GREEN end-to-end, then run a human verification checkpoint to validate the real multi-locador experience that automated tests cannot fully prove — the wizard UX and, critically, that a generated AC .docx with 2 cloned locador rows opens in Microsoft Word WITHOUT triggering the "repaired document" dialog (the Pitfall 1 risk).

Purpose: Close the goal-backward must-haves for CONTR-11 and CONTR-12 with both automated and human evidence. This is the phase acceptance gate.
Output: Verified green suite + human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contratos-multi-locador/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full contracts suite + typecheck — confirm GREEN</name>
  <files></files>
  <read_first>
    - tma/src/__tests__/contracts/fillPlaceholders.test.ts
    - tma/src/__tests__/contracts/extractPlaceholders.test.ts
    - tma/src/__tests__/contracts/generateRoute.test.ts
    - tma/src/__tests__/contracts/ContratoWizard.test.tsx
  </read_first>
  <action>
Run the complete contracts test suite and the TypeScript typecheck from tma/. If any test fails, diagnose and report which Plan's code is responsible — do NOT modify test expectations to force green (tests are the spec). Fix production code only.

Commands:
- `npm test -- --run contracts` (all contract tests)
- `npx tsc --noEmit` (typecheck)
- `npm run lint` (lint — fix any new warnings introduced by Phase 3 files)
  </action>
  <verify>
    <automated>cd tma && npm test -- --run contracts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run contracts` from tma/ reports 0 failing tests
    - Output contains the cloneLocadorRow, pluralizeLocadorRefs, multi-locador route, and multi-locador wizard describe blocks all passing
    - `npx tsc --noEmit` from tma/ exits 0
    - `npm run lint` from tma/ reports no errors in the four Phase 3 source files (fillPlaceholders.ts, ContratoWizard.tsx, route.ts, createFixtures.ts)
  </acceptance_criteria>
  <done>Full contracts suite green, typecheck clean, lint clean for Phase 3 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Multi-locador contract generation (CONTR-11, CONTR-12):
    - Wizard step 2 now has a "Locadores" section with "+ Agregar locador" and "−" remove buttons; each locador is a collapsible section with its own "Personas relacionadas" upload zone.
    - AC PF / AC PJ with 2+ locadores clones the identification table row per locador and fills each with that locador's data (N Gemini calls).
    - Adenda models with 2+ locadores rewrite "el/El LOCADOR" → "los/Los LOCADORES" in the prose before generation (single Gemini call).
  </what-built>
  <how-to-verify>
    Prerequisites: the 10 real .docx templates must be present in tma/templates/ and GEMINI_API_KEY set in the env. Run `npm run dev` from tma/ and log in.

    1. Go to /tma/contratos. Select "AC Persona Física".
    2. In step 2, confirm the "Locadores" section shows "Locador 1" with an upload zone and a "+ Agregar locador" button. There should be NO "−" button on Locador 1.
    3. Click "+ Agregar locador". Confirm a "Locador 2" section animates in and now BOTH locadores show a "−" button.
    4. Confirm "Generar contrato" is disabled (dimmed) until BOTH locadores have at least one file. Upload a file to Locador 1 only → still disabled. Upload a file to Locador 2 → becomes enabled.
    5. Upload distinct documentation for each locador (e.g. two different people's data) and click "Generar contrato". In step 3 confirm the messages cycle per locador: "Analizando documentación del Locador 1...", "...Locador 2...".
    6. Download the .docx and OPEN IT IN MICROSOFT WORD. CRITICAL: Word must NOT show a "We found a problem... repaired" dialog. Confirm the identification table has TWO locador rows, each populated with the corresponding locador's data.
    7. Repeat the remove flow: add a 3rd locador, then click its "−" — confirm it animates out and the count returns to expected.
    8. Now select an Adenda model (e.g. "Adenda Carta Oferta - PF en PESOS"), add a 2nd locador, upload site + person files, generate. Open the .docx and confirm prose reads "los LOCADORES" / "Los LOCADORES" where appropriate, while phrases like "del LOCADOR" / "al LOCADOR" remain in singular form, and clause numbering is unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe what failed (especially any Word "repaired" dialog, wrong data placement, or altered Adenda clauses).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated .docx → Microsoft Word | OOXML validity boundary — invalid paraIds would surface as a Word repair dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | cloned-row OOXML validity in real templates | mitigate | Human checkpoint step 6 verifies Word opens the 2-locador AC .docx with no repair dialog — proves unique paraIds hold on the REAL templates, not just fixtures. |
</threat_model>

<verification>
- Automated: `npm test -- --run contracts` green; `npx tsc --noEmit` exit 0.
- Human: wizard add/remove/validation behaves per D-01/D-03; AC 2-locador .docx opens cleanly in Word with both rows filled; Adenda prose pluralized without altering "del/al LOCADOR" or numbering.
</verification>

<success_criteria>
- Full contracts suite GREEN and typecheck clean.
- Human sign-off ("approved") on the multi-locador wizard + generated documents.
- CONTR-11 and CONTR-12 success criteria from ROADMAP demonstrably true.
</success_criteria>

<output>
After completion, create `.planning/phases/03-contratos-multi-locador/03-05-SUMMARY.md`
</output>
