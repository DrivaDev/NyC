---
phase: 04-polish-brand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/dashboard/CategoriesClient.tsx
  - components/dashboard/DishesClient.tsx
  - components/dashboard/CategoryModal.tsx
  - components/dashboard/DishModal.tsx
  - components/dashboard/RestaurantProfileForm.tsx
  - components/dashboard/OnboardingSlug.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-06

must_haves:
  truths:
    - "The CSS variable --color-brand-danger: #DC2626 is defined in @theme in globals.css"
    - "No red-* Tailwind class appears in CategoriesClient, DishesClient, CategoryModal, DishModal, RestaurantProfileForm, or OnboardingSlug"
    - "Delete buttons use border-brand-danger/30, text-brand-danger, hover:bg-brand-danger/10"
    - "Confirm-delete buttons use bg-brand-danger and hover:bg-[#B91C1C]"
    - "Error toasts use border-brand-danger/30 and text-brand-danger for the XCircle icon"
    - "Required-field asterisks use text-brand-danger"
    - "hover:bg-[#C2410C] on primary buttons is untouched"
  artifacts:
    - path: "app/globals.css"
      provides: "--color-brand-danger token in @theme block"
      contains: "--color-brand-danger: #DC2626"
    - path: "components/dashboard/CategoriesClient.tsx"
      provides: "Migrated danger classes"
    - path: "components/dashboard/DishesClient.tsx"
      provides: "Migrated danger classes"
    - path: "components/dashboard/CategoryModal.tsx"
      provides: "Migrated danger classes"
    - path: "components/dashboard/DishModal.tsx"
      provides: "Migrated danger classes"
    - path: "components/dashboard/RestaurantProfileForm.tsx"
      provides: "Migrated danger classes"
    - path: "components/dashboard/OnboardingSlug.tsx"
      provides: "Migrated danger classes"
  key_links:
    - from: "app/globals.css @theme"
      to: "Tailwind utilities bg-brand-danger / text-brand-danger / border-brand-danger"
      via: "@theme CSS custom property"
      pattern: "--color-brand-danger"
---

<objective>
Add the `--color-brand-danger` CSS token and replace every `red-*` Tailwind class across six component files with its `brand-danger` equivalent.

Purpose: Formalise the danger color into the brand design system so no off-palette color (`red-*`) appears anywhere in the codebase (BRAND-01, BRAND-06).
Output: Updated `globals.css` with the new token; six updated component files with zero `red-*` classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@menu-digital/.planning/PROJECT.md
@menu-digital/.planning/ROADMAP.md
@menu-digital/.planning/phases/04-polish-brand/04-CONTEXT.md
@menu-digital/.planning/phases/04-polish-brand/04-UI-SPEC.md

<interfaces>
<!-- Token being added to globals.css -->
Current @theme block in app/globals.css:
```css
@theme {
  --color-brand-principal: #EA580C;
  --color-brand-titulares: #9A3412;
  --color-brand-acento:    #FED7AA;
  --color-brand-fondo:     #FFF7ED;
  --color-brand-texto:     #1C1917;
  --font-sans: var(--font-fira-sans), sans-serif;
}
```
Add after --color-brand-texto line:
  --color-brand-danger:    #DC2626;

<!-- Complete class substitution table (from 04-UI-SPEC.md §3) -->
| Old class              | New class                    |
|------------------------|------------------------------|
| border-red-200         | border-brand-danger/30       |
| text-red-500           | text-brand-danger            |
| hover:bg-red-50        | hover:bg-brand-danger/10     |
| text-red-600           | text-brand-danger            |
| bg-red-600             | bg-brand-danger              |
| hover:bg-red-700       | hover:bg-[#B91C1C]           |
| hover:bg-red-50        | hover:bg-brand-danger/10     |
| hover:border-red-300   | hover:border-brand-danger/50 |
| bg-red-50              | bg-brand-danger/10           |
| text-red-800           | text-brand-danger            |

DO NOT change hover:bg-[#C2410C] — that is brand-principal hover, not red.
DO NOT change text-gray-300 in DishesClient — neutral placeholder, not red.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --color-brand-danger token to globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css — read the full file to see the current @theme block before editing
  </read_first>
  <action>
    Open `app/globals.css`. In the `@theme` block, insert the danger token on a new line after `--color-brand-texto`:

    ```css
    --color-brand-danger:    #DC2626;
    ```

    The resulting @theme block must be:
    ```css
    @theme {
      --color-brand-principal: #EA580C;
      --color-brand-titulares: #9A3412;
      --color-brand-acento:    #FED7AA;
      --color-brand-fondo:     #FFF7ED;
      --color-brand-texto:     #1C1917;
      --color-brand-danger:    #DC2626;
      --font-sans: var(--font-fira-sans), sans-serif;
    }
    ```

    No other changes to globals.css.
  </action>
  <verify>
    <automated>grep -c "\-\-color-brand-danger:    #DC2626" "menu-digital/app/globals.css"</automated>
  </verify>
  <done>globals.css contains `--color-brand-danger:    #DC2626` inside the @theme block; file has exactly 1 match.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all red-* classes in the six component files</name>
  <files>
    components/dashboard/CategoriesClient.tsx,
    components/dashboard/DishesClient.tsx,
    components/dashboard/CategoryModal.tsx,
    components/dashboard/DishModal.tsx,
    components/dashboard/RestaurantProfileForm.tsx,
    components/dashboard/OnboardingSlug.tsx
  </files>
  <read_first>
    - components/dashboard/CategoriesClient.tsx — read full file to locate all red-* occurrences
    - components/dashboard/DishesClient.tsx — read full file
    - components/dashboard/CategoryModal.tsx — read full file
    - components/dashboard/DishModal.tsx — read full file
    - components/dashboard/RestaurantProfileForm.tsx — read full file
    - components/dashboard/OnboardingSlug.tsx — read full file
  </read_first>
  <action>
    Apply the following substitutions in each file. Work file-by-file. Use the exact mappings — no interpretation.

    **CategoriesClient.tsx** (lines roughly 162, 172, 192, 227, 231 — verify actual positions after reading):
    - Delete button className: replace `border-red-200` → `border-brand-danger/30`, `text-red-500` → `text-brand-danger`, `hover:bg-red-50` → `hover:bg-brand-danger/10`
    - Inline label "¿Eliminar?": replace `text-red-600` → `text-brand-danger`
    - Confirm delete button: replace `bg-red-600` → `bg-brand-danger`, `hover:bg-red-700` → `hover:bg-[#B91C1C]`
    - Error toast `<div>`: replace `border-red-200` → `border-brand-danger/30`
    - Error toast `<XCircle>`: replace `text-red-500` → `text-brand-danger`

    **DishesClient.tsx** (same pattern as CategoriesClient):
    - Trash button: `border-red-200` → `border-brand-danger/30`, `text-red-500` → `text-brand-danger`, `hover:bg-red-50` → `hover:bg-brand-danger/10`
    - Inline label: `text-red-600` → `text-brand-danger`
    - Confirm button: `bg-red-600` → `bg-brand-danger`, `hover:bg-red-700` → `hover:bg-[#B91C1C]`
    - Error toast border: `border-red-200` → `border-brand-danger/30`
    - Error toast icon: `text-red-500` → `text-brand-danger`

    **CategoryModal.tsx**:
    - Required asterisk `<span>`: `text-red-500` → `text-brand-danger`
    - Form error `<p>`: `text-red-600` → `text-brand-danger`

    **DishModal.tsx**:
    - Required asterisk on Name field: `text-red-500` → `text-brand-danger`
    - Required asterisk on Price field: `text-red-500` → `text-brand-danger`
    - Required asterisk on Category field: `text-red-500` → `text-brand-danger`
    - Upload error text: `text-red-600` → `text-brand-danger`
    - Form error message: `text-red-600` → `text-brand-danger`

    **RestaurantProfileForm.tsx**:
    - Required asterisk: `text-red-500` → `text-brand-danger`
    - Image remove button: `hover:bg-red-50` → `hover:bg-brand-danger/10`, `hover:border-red-300` → `hover:border-brand-danger/50`
    - Upload error text: `text-red-600` → `text-brand-danger`
    - Error banner bg: `bg-red-50` → `bg-brand-danger/10`
    - Error banner border: `border-red-200` → `border-brand-danger/30`
    - Error banner text: `text-red-800` → `text-brand-danger`

    **OnboardingSlug.tsx**:
    - Inline validation error: `text-red-600` → `text-brand-danger`
    - Error toast border: `border-red-200` → `border-brand-danger/30`
    - Error toast icon: `text-red-500` → `text-brand-danger`

    SAFETY: Do NOT change `hover:bg-[#C2410C]` anywhere — that is the brand-principal hover for primary buttons.
    SAFETY: Do NOT change `text-gray-300` in DishesClient — it is a neutral placeholder color for ImageOff icon.
  </action>
  <verify>
    <automated>grep -rn "red-" menu-digital/components/dashboard/CategoriesClient.tsx menu-digital/components/dashboard/DishesClient.tsx menu-digital/components/dashboard/CategoryModal.tsx menu-digital/components/dashboard/DishModal.tsx menu-digital/components/dashboard/RestaurantProfileForm.tsx menu-digital/components/dashboard/OnboardingSlug.tsx</automated>
  </verify>
  <done>
    The grep command returns zero matches — no `red-` string remains in any of the six files.
    `hover:bg-[#C2410C]` and `text-gray-300` are the only color-class exceptions allowed to remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token | Client-rendered CSS only; no user data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | globals.css @theme | accept | CSS token change is purely visual; no auth or data boundary crossed |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "\-\-color-brand-danger" menu-digital/app/globals.css` returns `1`
2. `grep -rn "red-" menu-digital/components/dashboard/CategoriesClient.tsx menu-digital/components/dashboard/DishesClient.tsx menu-digital/components/dashboard/CategoryModal.tsx menu-digital/components/dashboard/DishModal.tsx menu-digital/components/dashboard/RestaurantProfileForm.tsx menu-digital/components/dashboard/OnboardingSlug.tsx` returns no lines
3. `grep -c "C2410C" menu-digital/components/dashboard/CategoriesClient.tsx` still returns `1` (primary hover untouched)
</verification>

<success_criteria>
- `app/globals.css` defines `--color-brand-danger: #DC2626` in the @theme block
- Zero `red-*` Tailwind classes in all six migrated files
- `hover:bg-[#C2410C]` and `text-gray-300` remain untouched in their respective files
- TypeScript compilation passes (`npx tsc --noEmit` exits 0)
</success_criteria>

<output>
After completion, create `menu-digital/.planning/phases/04-polish-brand/04-01-SUMMARY.md` following the summary template.
</output>
