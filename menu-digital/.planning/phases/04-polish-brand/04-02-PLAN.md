---
phase: 04-polish-brand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(marketing)/page.tsx
autonomous: true
requirements:
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - BRAND-05

must_haves:
  truths:
    - "Visiting / shows a centered card on bg-brand-fondo background"
    - "The page has an H1 'Menú Digital' in text-2xl font-bold text-brand-titulares"
    - "A primary orange CTA links to /sign-in with text 'Iniciar sesión'"
    - "A secondary outlined CTA links to /sign-up with text 'Crear cuenta'"
    - "Both CTA buttons have min-h-[44px] touch target"
    - "The footer reads 'Desarrollado por Driva Dev' with Driva Dev in font-bold"
    - "No Clerk component is embedded on this page"
    - "No gradients, no off-palette colors, no excessive shadows"
  artifacts:
    - path: "app/(marketing)/page.tsx"
      provides: "Branded landing page component"
      contains: "Menú Digital"
  key_links:
    - from: "app/(marketing)/page.tsx"
      to: "/sign-in"
      via: "<a href> CTA link"
      pattern: "href=\"/sign-in\""
    - from: "app/(marketing)/page.tsx"
      to: "/sign-up"
      via: "<a href> CTA link"
      pattern: "href=\"/sign-up\""
---

<objective>
Replace the placeholder landing page at `app/(marketing)/page.tsx` with a fully-branded minimal card: wordmark, tagline, and two CTA links.

Purpose: Deliver the public entry point for the product with Driva Dev visual identity (BRAND-02, BRAND-03, BRAND-04, BRAND-05). First impression for restaurant owners arriving at menudig.com.ar.
Output: `app/(marketing)/page.tsx` rewritten as a static Server Component with the exact markup from 04-UI-SPEC.md §1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@menu-digital/.planning/PROJECT.md
@menu-digital/.planning/ROADMAP.md
@menu-digital/.planning/phases/04-polish-brand/04-CONTEXT.md
@menu-digital/.planning/phases/04-polish-brand/04-UI-SPEC.md

<interfaces>
<!-- Current file to be replaced — read before writing -->
Current app/(marketing)/page.tsx:
```tsx
export default function MarketingPage() {
  return (
    <div className="min-h-screen bg-brand-fondo flex items-center justify-center">
      <p className="text-brand-texto">Landing page — disponible en la Fase 4</p>
    </div>
  )
}
```

<!-- Target markup from 04-UI-SPEC.md §1 — implement this exactly -->
The complete JSX to produce:
- Outer: <main className="min-h-screen bg-brand-fondo flex flex-col items-center justify-center px-4">
- Inner card: <div className="w-full max-w-sm flex flex-col items-center gap-6 text-center">
- H1: <h1 className="text-2xl font-bold text-brand-titulares">Menú Digital</h1>
- Tagline: <p className="text-sm font-normal text-brand-texto">El menú digital de tu restaurante, accesible desde cualquier celular.</p>
- Buttons wrapper: <div className="flex flex-col gap-3 w-full">
- Primary CTA: <a href="/sign-in" className="w-full flex items-center justify-center bg-brand-principal text-white text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-[#C2410C] focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150">Iniciar sesión</a>
- Secondary CTA: <a href="/sign-up" className="w-full flex items-center justify-center border border-brand-principal text-brand-principal text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-brand-fondo focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150">Crear cuenta</a>
- Footer: <footer className="absolute bottom-6 text-center"><p className="text-xs font-light text-brand-texto">Desarrollado por <strong className="font-bold">Driva Dev</strong></p></footer>

Rules:
- No Clerk component on this page. <a href> links only — no <Link> needed (external-looking routes).
- No feature list, no hero image, no gradients, no shadow beyond what bg-white cards use.
- This is a Server Component — no 'use client' directive.
- The function name can stay MarketingPage or be renamed HomePage — either is fine.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite landing page with branded card markup</name>
  <files>app/(marketing)/page.tsx</files>
  <read_first>
    - app/(marketing)/page.tsx — read current placeholder content before overwriting
  </read_first>
  <action>
    Rewrite `app/(marketing)/page.tsx` completely with the following content. No imports are needed (no Clerk, no Link, no icons):

    ```tsx
    export default function MarketingPage() {
      return (
        <main className="min-h-screen bg-brand-fondo flex flex-col items-center justify-center px-4">
          <div className="w-full max-w-sm flex flex-col items-center gap-6 text-center">
            <h1 className="text-2xl font-bold text-brand-titulares">Menú Digital</h1>
            <p className="text-sm font-normal text-brand-texto">
              El menú digital de tu restaurante, accesible desde cualquier celular.
            </p>
            <div className="flex flex-col gap-3 w-full">
              <a
                href="/sign-in"
                className="w-full flex items-center justify-center bg-brand-principal text-white text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-[#C2410C] focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150"
              >
                Iniciar sesión
              </a>
              <a
                href="/sign-up"
                className="w-full flex items-center justify-center border border-brand-principal text-brand-principal text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-brand-fondo focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150"
              >
                Crear cuenta
              </a>
            </div>
          </div>

          <footer className="absolute bottom-6 text-center">
            <p className="text-xs font-light text-brand-texto">
              Desarrollado por <strong className="font-bold">Driva Dev</strong>
            </p>
          </footer>
        </main>
      )
    }
    ```

    Do not add 'use client'. Do not import anything. Do not add metadata export (the parent layout handles that).
  </action>
  <verify>
    <automated>grep -c "Iniciar sesión" "menu-digital/app/(marketing)/page.tsx"</automated>
  </verify>
  <done>
    - File contains `Iniciar sesión` (primary CTA text)
    - File contains `href="/sign-in"`
    - File contains `href="/sign-up"`
    - File contains `Desarrollado por`
    - File contains `bg-brand-principal` (primary button bg)
    - File contains `min-h-[44px]` (touch target)
    - File does NOT contain `'use client'`
    - File does NOT contain the placeholder text "disponible en la Fase 4"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public page | No auth, no user data, no form submission — static HTML links only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Landing page | accept | Page is intentionally public; links go to Clerk-hosted sign-in/sign-up which handle all auth security |
</threat_model>

<verification>
After task completes:
1. `grep -c "Iniciar sesión" "menu-digital/app/(marketing)/page.tsx"` returns `1`
2. `grep -c "Crear cuenta" "menu-digital/app/(marketing)/page.tsx"` returns `1`
3. `grep -c "Driva Dev" "menu-digital/app/(marketing)/page.tsx"` returns `1`
4. `grep "use client" "menu-digital/app/(marketing)/page.tsx"` returns no match
5. `grep "disponible en la Fase 4" "menu-digital/app/(marketing)/page.tsx"` returns no match
</verification>

<success_criteria>
- `app/(marketing)/page.tsx` renders the branded card with H1, tagline, two CTAs, and footer
- Primary CTA (`href="/sign-in"`) uses `bg-brand-principal` with `hover:bg-[#C2410C]`
- Secondary CTA (`href="/sign-up"`) uses `border-brand-principal text-brand-principal`
- Both CTAs have `min-h-[44px]`
- Footer contains "Desarrollado por Driva Dev" with `font-bold` on "Driva Dev"
- No Clerk component, no gradient, no off-palette color
- TypeScript compilation passes (`npx tsc --noEmit` exits 0)
</success_criteria>

<output>
After completion, create `menu-digital/.planning/phases/04-polish-brand/04-02-SUMMARY.md` following the summary template.
</output>
