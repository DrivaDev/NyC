---
phase: 04-polish-brand
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - components/dashboard/QRCard.tsx
  - app/(admin)/dashboard/qr/page.tsx
  - app/(admin)/dashboard/page.tsx
  - components/dashboard/Sidebar.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - BRAND-05
  - BRAND-06

must_haves:
  truths:
    - "Navigating to /dashboard/qr shows the QR code, download button, and 'Ver mi menú' link"
    - "The Mi QR sidebar nav item is enabled (no 'Pronto' badge, link is clickable)"
    - "The dashboard home page (/dashboard) is unchanged — QR card still appears there"
    - "QRCard.tsx is a Server Component (no 'use client') accepting menuUrl, qrDataUrl, slug props"
    - "The /dashboard/qr page has H1 'Mi QR' in text-2xl font-bold text-brand-titulares"
    - "The QR download button uses bg-brand-principal with hover:bg-[#C2410C]"
    - "The footer 'Desarrollado por Driva Dev' appears on /dashboard/qr via the shared DashboardShell"
  artifacts:
    - path: "components/dashboard/QRCard.tsx"
      provides: "Extracted server component with QR preview, download button, and Ver mi menú link"
      contains: "qrDataUrl"
    - path: "app/(admin)/dashboard/qr/page.tsx"
      provides: "Dedicated QR page rendering QRCard"
      contains: "QRCard"
    - path: "app/(admin)/dashboard/page.tsx"
      provides: "Dashboard home using QRCard component instead of inline QR JSX"
      contains: "QRCard"
    - path: "components/dashboard/Sidebar.tsx"
      provides: "Mi QR nav item enabled (enabled: true)"
  key_links:
    - from: "app/(admin)/dashboard/qr/page.tsx"
      to: "components/dashboard/QRCard.tsx"
      via: "import and JSX usage"
      pattern: "QRCard"
    - from: "app/(admin)/dashboard/page.tsx"
      to: "components/dashboard/QRCard.tsx"
      via: "import and JSX usage"
      pattern: "QRCard"
    - from: "components/dashboard/Sidebar.tsx Mi QR navItem"
      to: "/dashboard/qr"
      via: "enabled: true Link"
      pattern: "enabled: true"
---

<objective>
Extract the QR card JSX from `dashboard/page.tsx` into a `QRCard.tsx` server component; create the `/dashboard/qr` page using QRCard; update the dashboard home to use QRCard; and enable the Mi QR nav item in Sidebar.tsx.

Purpose: Give the QR code a dedicated page at `/dashboard/qr` (D-QR-01, D-QR-02), enable the Sidebar nav item (D-QR-02), and keep the dashboard home unchanged via the extracted component (D-QR-03). Covers BRAND-01..06 visual requirements for the new page.
Output: Three new/modified files + Sidebar nav update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@menu-digital/.planning/PROJECT.md
@menu-digital/.planning/ROADMAP.md
@menu-digital/.planning/phases/04-polish-brand/04-CONTEXT.md
@menu-digital/.planning/phases/04-polish-brand/04-UI-SPEC.md
@menu-digital/.planning/phases/04-polish-brand/04-03-SUMMARY.md

<interfaces>
<!-- Current dashboard/page.tsx QR card section — the JSX to extract into QRCard.tsx -->
The QR card block (inside the grid div, first column):
```tsx
{/* QR preview card */}
<div className="bg-white rounded-lg shadow-sm border border-brand-acento p-6 flex flex-col items-center gap-4">
  <h2 className="text-base font-bold text-brand-titulares self-start">
    Tu QR
  </h2>

  {/* QR image */}
  <div className="p-3 border border-brand-acento rounded-lg">
    {/* eslint-disable-next-line @next/next/no-img-element */}
    <img src={qrDataUrl} alt="QR de tu menú" width={200} height={200} />
  </div>

  {/* Menu URL — clickable link opens menu in new tab (QR-03) */}
  <a
    href={menuUrl}
    target="_blank"
    rel="noopener noreferrer"
    className="w-full flex items-center gap-2 bg-brand-fondo rounded-md px-4 py-3 hover:bg-brand-acento transition-colors duration-150 group"
  >
    <span className="font-mono text-xs text-brand-texto truncate group-hover:text-brand-titulares">
      {menuUrl}
    </span>
  </a>

  {/* Download button */}
  <a
    href={qrDataUrl}
    download={`qr-${slug}.png`}
    className="w-full flex items-center justify-center gap-2 bg-brand-principal text-white text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-[#C2410C] focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150"
  >
    <Download size={16} />
    Descargar QR
  </a>

  {/* Open menu in new tab — secondary affordance (QR-03) */}
  <a
    href={menuUrl}
    target="_blank"
    rel="noopener noreferrer"
    className="w-full flex items-center justify-center gap-2 border border-brand-principal text-brand-principal text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-brand-fondo focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150"
  >
    Ver mi menú
  </a>
</div>
```

<!-- QRCard props interface from 04-UI-SPEC.md §4 -->
interface QRCardProps {
  menuUrl: string
  qrDataUrl: string
  slug: string
}

<!-- /dashboard/qr page layout from 04-UI-SPEC.md §4 -->
Page structure:
<div className="flex flex-col gap-8">
  <div>
    <h1 className="text-2xl font-bold text-brand-titulares mb-1">Mi QR</h1>
    <p className="text-sm font-normal text-brand-texto">
      Descargá tu código QR o compartí el link de tu menú.
    </p>
  </div>
  <div className="max-w-sm">
    <QRCard menuUrl={menuUrl} qrDataUrl={qrDataUrl} slug={restaurant.slug} />
  </div>
</div>

<!-- dashboard/page.tsx replacement for the QR card inline JSX -->
After extraction, the QR card div in the grid becomes:
<QRCard menuUrl={menuUrl} qrDataUrl={qrDataUrl} slug={restaurant.slug} />

(The outer grid wrapper and the "Acciones rápidas" card remain unchanged.)

<!-- Current Sidebar.tsx Mi QR navItem (from prior plan output) -->
The navItems array currently has:
{
  label: 'Mi QR',
  href: '/dashboard/qr',
  icon: <QrCode size={16} />,
  enabled: false,   // ← change to true
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QRCard.tsx server component and update dashboard/page.tsx</name>
  <files>
    components/dashboard/QRCard.tsx,
    app/(admin)/dashboard/page.tsx
  </files>
  <read_first>
    - app/(admin)/dashboard/page.tsx — read full file; locate the QR card JSX block inside the grid (first column), and note the import list at the top
  </read_first>
  <action>
    **Step A — Create components/dashboard/QRCard.tsx (new file):**

    This is a Server Component — no `'use client'`. Copy the QR card JSX block from dashboard/page.tsx verbatim; replace `restaurant.slug` reference in the download filename with the `slug` prop:

    ```tsx
    import { Download } from 'lucide-react'

    interface QRCardProps {
      menuUrl: string
      qrDataUrl: string
      slug: string
    }

    export default function QRCard({ menuUrl, qrDataUrl, slug }: QRCardProps) {
      return (
        <div className="bg-white rounded-lg shadow-sm border border-brand-acento p-6 flex flex-col items-center gap-4">
          <h2 className="text-base font-bold text-brand-titulares self-start">
            Tu QR
          </h2>

          {/* QR image */}
          <div className="p-3 border border-brand-acento rounded-lg">
            {/* eslint-disable-next-line @next/next/no-img-element */}
            <img src={qrDataUrl} alt="QR de tu menú" width={200} height={200} />
          </div>

          {/* Menu URL — clickable link opens menu in new tab */}
          <a
            href={menuUrl}
            target="_blank"
            rel="noopener noreferrer"
            className="w-full flex items-center gap-2 bg-brand-fondo rounded-md px-4 py-3 hover:bg-brand-acento transition-colors duration-150 group"
          >
            <span className="font-mono text-xs text-brand-texto truncate group-hover:text-brand-titulares">
              {menuUrl}
            </span>
          </a>

          {/* Download button */}
          <a
            href={qrDataUrl}
            download={`qr-${slug}.png`}
            className="w-full flex items-center justify-center gap-2 bg-brand-principal text-white text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-[#C2410C] focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150"
          >
            <Download size={16} />
            Descargar QR
          </a>

          {/* Ver mi menú — secondary affordance */}
          <a
            href={menuUrl}
            target="_blank"
            rel="noopener noreferrer"
            className="w-full flex items-center justify-center gap-2 border border-brand-principal text-brand-principal text-sm font-medium rounded-lg px-4 py-3 min-h-[44px] hover:bg-brand-fondo focus:outline-none focus:ring-2 focus:ring-brand-principal focus:ring-offset-2 transition-colors duration-150"
          >
            Ver mi menú
          </a>
        </div>
      )
    }
    ```

    **Step B — Modify app/(admin)/dashboard/page.tsx:**

    1. Add import at the top: `import QRCard from '@/components/dashboard/QRCard'`
    2. Remove the `Download` import from lucide-react (it moves to QRCard.tsx). Keep `Loader2`, `Tag`, `UtensilsCrossed`.
    3. In the JSX grid section, replace the entire QR card `<div className="bg-white rounded-lg...">...</div>` block (the first grid column) with:
       ```tsx
       <QRCard menuUrl={menuUrl} qrDataUrl={qrDataUrl} slug={restaurant.slug} />
       ```
    4. Everything else in dashboard/page.tsx — auth, db, QRCode.toDataURL, menuUrl const, the welcome heading, the grid wrapper, the "Acciones rápidas" card, the OnboardingSlug states — stays exactly as-is.
  </action>
  <verify>
    <automated>grep -c "qrDataUrl" "menu-digital/components/dashboard/QRCard.tsx"</automated>
  </verify>
  <done>
    - QRCard.tsx exists, contains `qrDataUrl` prop usage, contains `Descargar QR` text, contains `Ver mi menú` text
    - QRCard.tsx does NOT contain `'use client'`
    - dashboard/page.tsx imports QRCard and uses `<QRCard menuUrl={menuUrl} qrDataUrl={qrDataUrl} slug={restaurant.slug} />`
    - dashboard/page.tsx no longer imports `Download` from lucide-react (moved to QRCard)
    - dashboard/page.tsx still contains `Acciones rápidas` (quick actions card unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /dashboard/qr page and enable Mi QR nav item</name>
  <files>
    app/(admin)/dashboard/qr/page.tsx,
    components/dashboard/Sidebar.tsx
  </files>
  <read_first>
    - components/dashboard/Sidebar.tsx — read full file; locate the Mi QR navItem with enabled: false; note the onNavigate prop added in Plan 03
  </read_first>
  <action>
    **Step A — Create app/(admin)/dashboard/qr/page.tsx (new file):**

    This is a Server Component. It replicates the auth + db pattern from dashboard/page.tsx to get the restaurant and generate the QR:

    ```tsx
    import { auth } from '@clerk/nextjs/server'
    import { redirect } from 'next/navigation'
    import QRCode from 'qrcode'
    import { dbConnect } from '@/lib/dbConnect'
    import { Restaurant } from '@/models/Restaurant'
    import QRCard from '@/components/dashboard/QRCard'

    export default async function QRPage() {
      const { userId } = await auth()
      if (!userId) redirect('/sign-in')

      await dbConnect()
      const restaurant = await Restaurant.findOne({ clerkId: userId }).lean<{
        _id: string
        name: string
        slug: string
        slugConfirmed: boolean
      }>()

      if (!restaurant || !restaurant.slugConfirmed) {
        redirect('/dashboard')
      }

      const appUrl = process.env.NEXT_PUBLIC_APP_URL ?? 'https://menudig.com.ar'
      const menuUrl = `${appUrl}/menu/${restaurant.slug}`
      const qrDataUrl = await QRCode.toDataURL(menuUrl, {
        width: 256,
        margin: 2,
        color: { dark: '#1C1917', light: '#FFFFFF' },
      })

      return (
        <div className="flex flex-col gap-8">
          <div>
            <h1 className="text-2xl font-bold text-brand-titulares mb-1">Mi QR</h1>
            <p className="text-sm font-normal text-brand-texto">
              Descargá tu código QR o compartí el link de tu menú.
            </p>
          </div>

          <div className="max-w-sm">
            <QRCard menuUrl={menuUrl} qrDataUrl={qrDataUrl} slug={restaurant.slug} />
          </div>
        </div>
      )
    }
    ```

    Note on the redirect guard: if restaurant is missing or slug not yet confirmed, redirect to /dashboard (the onboarding flow handles slug confirmation). This prevents showing a blank QR page during onboarding.

    **Step B — Modify components/dashboard/Sidebar.tsx:**

    In the `navItems` array, find the Mi QR item:
    ```tsx
    {
      label: 'Mi QR',
      href: '/dashboard/qr',
      icon: <QrCode size={16} />,
      enabled: false,   // ← change this
    },
    ```
    Change `enabled: false` to `enabled: true`.

    No other changes to Sidebar.tsx. The `onNavigate` prop added in Plan 03 is already present; do not remove it.
  </action>
  <verify>
    <automated>grep -c "Mi QR" "menu-digital/app/(admin)/dashboard/qr/page.tsx"</automated>
  </verify>
  <done>
    - app/(admin)/dashboard/qr/page.tsx exists and contains "Mi QR" heading text
    - app/(admin)/dashboard/qr/page.tsx imports QRCard and renders `<QRCard ... />`
    - app/(admin)/dashboard/qr/page.tsx contains `redirect('/dashboard')` guard for unauthenticated/onboarding state
    - Sidebar.tsx Mi QR navItem has `enabled: true`
    - Sidebar.tsx no longer renders the "Pronto" badge for Mi QR (enabled items skip the disabled span branch)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → DB | /dashboard/qr/page.tsx calls auth() and filters Restaurant by clerkId — same pattern as all other dashboard pages |
| Public QR URL | menuUrl embedded in QR image uses NEXT_PUBLIC_APP_URL — intentionally public, points to the public menu page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Elevation of Privilege | /dashboard/qr/page.tsx auth guard | mitigate | `await auth()` + `redirect('/sign-in')` on userId absence — identical pattern to dashboard/page.tsx; Clerk middleware provides an outer layer |
| T-04-04-02 | Information Disclosure | QRCard qrDataUrl | accept | qrDataUrl is a data URI of the QR image (public menu URL encoded); no secrets or PII are embedded in the QR |
| T-04-04-03 | Tampering | Restaurant.findOne({ clerkId: userId }) | mitigate | DB query always scoped to authenticated userId — user cannot access another restaurant's QR by manipulating the URL |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "qrDataUrl" "menu-digital/components/dashboard/QRCard.tsx"` returns `3` or more
2. `grep "use client" "menu-digital/components/dashboard/QRCard.tsx"` returns no match
3. `grep -c "QRCard" "menu-digital/app/(admin)/dashboard/page.tsx"` returns `2` (import + JSX usage)
4. `grep -c "Mi QR" "menu-digital/app/(admin)/dashboard/qr/page.tsx"` returns `1`
5. `grep "enabled: false" "menu-digital/components/dashboard/Sidebar.tsx"` returns no match (all items now enabled)
6. `grep -c "enabled: true" "menu-digital/components/dashboard/Sidebar.tsx"` returns `4` (all four nav items now enabled, including Mi QR)
7. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- QRCard.tsx is a server component with props `{ menuUrl, qrDataUrl, slug }` rendering the full QR card
- /dashboard/qr page shows H1 "Mi QR", subtitle, and QRCard constrained to max-w-sm
- /dashboard/qr page redirects to /dashboard if restaurant not found or slug not confirmed
- dashboard/page.tsx uses `<QRCard>` for the QR card — no inline QR JSX duplication
- Mi QR Sidebar nav item is enabled; "Pronto" badge is gone; link navigates to /dashboard/qr
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `menu-digital/.planning/phases/04-polish-brand/04-04-SUMMARY.md` following the summary template.
</output>
